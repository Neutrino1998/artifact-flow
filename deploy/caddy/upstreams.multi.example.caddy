# Shape rendered by the Ansible executor on a multi-host LB release. Example
# only; the running config imports upstreams.caddy.
(backend_upstream_targets) {
	to app1.internal:8000 app2.internal:8000
}

(frontend_upstream_targets) {
	to app1.internal:3000 app2.internal:3000
}
