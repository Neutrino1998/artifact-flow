# ========================================
# 核心依赖
# ========================================

# LangChain 核心组件
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0  # OpenAI/兼容接口支持
langgraph>=0.2.0  # 工作流编排
langgraph-checkpoint>=2.0.0  # 状态管理

# ========================================
# 模型提供商 (可选，按需安装)
# ========================================

# langchain-anthropic>=0.2.0  # Claude 支持 (如需要请取消注释)
# langchain-community>=0.3.0  # 其他社区集成 (如需要请取消注释)

# ========================================
# 工具依赖
# ========================================

# 环境变量管理
python-dotenv>=1.0.0