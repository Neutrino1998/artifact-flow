# ========================================
# 核心依赖
# ========================================

# LiteLLM - 统一 LLM 接口
# 支持 OpenAI/DeepSeek/Qwen/Ollama/vLLM 等 100+ 提供商
litellm>=1.82.4,<2.0.0,!=1.82.7,!=1.82.8

# ========================================
# 工具依赖
# ========================================

# 环境变量管理
python-dotenv>=1.0.0

# 网络抓取
beautifulsoup4>=4.11.0  # HTML解析器：Jina Reader API fallback
aiohttp>=3.14.0  # 异步HTTP客户端（3.14.0 修 CVE-2026-34993 / CVE-2026-47265）

# 文档转换
pymupdf>=1.24.0  # PDF文本提取
charset-normalizer>=3.0.0  # 文本编码检测
python-multipart>=0.0.9  # 文件上传支持

# 图片处理：上传校验（按内容探测 png/jpeg + 解压炸弹闸）+ 识图 read 路径降采样
Pillow>=10.4.0

# 可观测性（PR-obs-lite sampler：进程 RSS / CPU% / FD / disk usage）
psutil>=5.9.0

# 沙盒（C 阶段）：Docker daemon HTTP API 的 asyncio 原生客户端。
# 选它因容器生灭要 await、直接挂引擎的 asyncio.Task.cancel() 取消/超时/lease 栈；
# docker CLI / 同步 docker-py 与之对不上。调用全部收口在 SandboxSession seam。
aiodocker>=0.24.0

# 文本模糊匹配（update_artifact Layer 2，锚定 + 有界 Levenshtein 校验）
rapidfuzz>=3.0.0

# RE2 正则引擎（grep_artifact）—— 线性时间、无回溯，结构性免疫 ReDoS。
# 替代 Python `re` 回溯引擎：LLM 可控 pattern 不再能用灾难性回溯卡死事件循环。
# 自带 manylinux wheel（打包 abseil），装进 python:3.11-slim 容器无需系统库。
google-re2>=1.1.0

# YAML 解析（Agent MD 文件）
pyyaml>=6.0

# ========================================
# 数据库
# ========================================

# SQLAlchemy ORM（异步支持）
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0

# 可选数据库驱动（MySQL / PostgreSQL）
aiomysql>=0.2.0
asyncpg>=0.29.0

# 数据库迁移
alembic>=1.13.0

# Redis（运行时状态 + 流传输）
redis>=5.0.0,<6.0.0

# ========================================
# API 依赖
# ========================================

# Web 框架
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# 配置管理
pydantic-settings>=2.0.0

# 异步文件操作（预留）
aiofiles>=23.2.0

# 异步 HTTP 客户端（http_tool + CLI api_client 运行时使用）
httpx>=0.26.0

# ========================================
# 认证依赖
# ========================================

# JWT Token
PyJWT>=2.8.0
# 密码哈希
bcrypt>=4.1.0

# ========================================
# CLI 依赖
# ========================================

# 命令行框架
typer>=0.9.0
# 终端美化
rich>=13.0.0
