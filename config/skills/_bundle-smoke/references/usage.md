# Using wordcount.py

After the skill is mounted into the sandbox, run:

```bash
python /workspace/.skills/bundle-smoke/scripts/wordcount.py /workspace/<some-artifact>
```

It prints a single integer (the whitespace-separated word count) to stdout.
