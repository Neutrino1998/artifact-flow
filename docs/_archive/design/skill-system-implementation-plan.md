# Skill 系统 + 工具渐进式披露 —— 实施计划

> 状态:规划完成,实现未启动
> 起草:2026-06-16 · 最后更新:2026-06-17
> 前序产物:
> - `sandbox-implementation-plan.md`(本目录)—— 沙盒主线(A/B/C/D 全完成);本 plan 的执行底座,skill 脚本/asset 全部跑在沙盒里。原则 7「依赖三层离线投递」直接被本 plan 的依赖模型继承。
> - `tool-result-artifact-mount.md`(本目录)—— 工具结果溢出转 artifact 的先例(`source` 字段 / 自动命名兜底),本 plan A 阶段是它的「具名一等通道」升级。
> - memory:`tool-ecosystem-positioning`(工具生态定位,两大引擎缺口)、`skill-standard-adoption-direction`(沙盒先→标准 Agent Skills、XML 非障碍、market 离线)、`skill-system-research`(Phase 9 早期研究)。
> 调研基线(2026-06-16,本 plan 起草前做):
> - **参考实现** `../custom-claude-code/build-output`(本仓库同级,非项目内):确证 ① Skill 与工具披露是**两套独立机制**(`SkillTool` 零引用 deferral);② 工具披露 = deferred-tools + `ToolSearch`(按名注入 system-reminder,`select:`/关键词加载,`tool_reference` 块由 API 端展开);③ MCP 工具一律 deferred、`mcp__server__tool` 命名,走同一 `ToolSearch`;④ 统一 registry = 归一化到一个 `Tool` 形 + 合并函数,provider 区别仅留 `isMcp`/`source` flag。
> - **开放标准** agentskills.io 已多厂商(Cursor/Gemini/Codex/Copilot):`SKILL.md` 文件夹=单元,frontmatter **恰 6 字段**(`name`/`description`/`license`/`compatibility`/`metadata`/`allowed-tools`),`version` 归 `metadata`,绝大多数真实 skill 只用 `name`+`description`(触发逻辑塞进 description)。
> - **生态弱点 = 依赖与离线**:社区默认运行时 `pip install`(撞 `ModuleNotFoundError` 反应式联网装),对气隙网敌对 —— 本系统**领先于标准**:`compatibility` 声明 + 预置环境 + 自带 wheel + ZIP-of-folder 是更稳的路。validator 工具成熟(`skills-ref` 官方 / `skill-validator` 社区:孤儿文件/未闭合 fence/token 预算/链接解析),无 trust 层 = 导入门禁是我方责任。
> - **用户存货** `../utils/claude-skills/anthropic-skills`(本仓库同级,非项目内;仅供参考,预装前先改):8 个 skill,5 脚本型(docx/pptx/xlsx/pdf/skill-creator)+ 3 纯 prose;docx/pptx/xlsx **三份重复** `scripts/office/` + ~40 `.xsd`;依赖全靠预置环境(lxml/openpyxl/pypdf/pandas/Pillow + libreoffice/poppler/pandoc/qpdf + node),无 manifest;无 `$ARGUMENTS`/`${SKILL_DIR}` 替换;frontmatter 实际只 `name`/`description`(+`license`)。schedule/setup-cowork 驱动 CC 产品 widget = **不可移植**,真实目标存货 = 4 文档 skill(+ skill-creator 改造)。

## 本文档定位

这是一份 **plan,不是详细设计**。讲清每个阶段做什么、为什么、什么算完成;**落实细节(schema 字段、具体改哪些代码)留到开工那个阶段再敲定**。同时是**跨 session 跟踪文档**:新 session 先读「进度」一节知道做到哪、下一步从哪续。每推进一阶段,更新状态 + 「变更日志」追加结论;方向有变也记日志。

本 plan 覆盖三件相互咬合的事:**skill 系统**、**工具渐进式披露** 与 **MCP client**。一起设计的理由是「30-endpoint 平台怎么渐进式披露」这个问题 —— 它的答案(披露归工具层 = tool-set provider + `search_tools`,见原则 1)既划定了 skill 与工具的职责边界,也直接成了 MCP 接入的地基(MCP server = 又一个 deferred tool-set provider)。三者共用同一套 provider/披露抽象。

## 进度

- **当前**:**规划完成,未开工。** 沙盒主线(底座)已收官。架构主分叉已收口(披露归工具层 / 可见性=visibility+default_enabled+稀疏覆盖 / 依赖三层离线 / 导入硬软双门 / read+mount 复用零新机制 / 部门授权统一 `department_resource_rule` 一张 polymorphic 表[方向归单元 visibility:public→减/department→加] / 工具权限两轴[等级归工具定义 · 成员=MD ± 部门规则 + skill-enable]),细节留各阶段开工敲定。
- **下一步**:**A 阶段(工具结果→富格式 artifact)** —— 引擎前置、独立可做、对沙盒数据入境有独立价值(memory `tool-ecosystem-positioning` 缺口①)。但注意:skill MVP(文档 skill 跑在用户上传上)**不阻塞于 A**(上传已是 artifact、mount 已通);只有「数据工具→artifact→沙盒」类场景(DB→CSV→分析)才硬依赖 A。排序仍 A 先,因为它最小、最独立、且是后续富数据流入的脊柱第一环。
- **分支策略(已定:走 main)**:与沙盒 plan 不同 —— 沙盒走 `feat/sandbox` 不增量合 main 是因为有「半迁移态(md→Word 过渡)漏到生产」的风险。本 plan **无此类破坏性中间态**,A/B/C 是纯加法引擎/存储特性,故**逐阶段直接合 main、再按既有策略 overlay intranet**(遵 `feedback-branch-strategy`),不开长命特性分支。

| 阶段 | 内容 | 状态 |
|---|---|---|
| A | 工具结果→富格式 artifact(`create_from_upload` 的第三调用方) | 未开始 |
| B | 工具渐进式披露(tool-set 文件格式 + `search_tools` 内建工具;MCP 适配缝) | 未开始 |
| C | Skill 核心(存储 + L1 注入 + `read_skill` + 权限/上下文覆盖 + 部门授权解析地基) | 未开始 |
| D | Skill bundle 执行(L3 挂载进沙盒 + `compatibility` 依赖三层 + 离线 wheel) | 未开始 |
| E | 导入门禁与预装(硬门槛 validator + 软门槛 verify agent + pandoc-first 预装) | 未开始 |
| F | MCP client(传输/协议客户端 + JSON-Schema→XML 适配 + provider 接入 B 的 deferred 披露) | 未开始 |
| G | 部门作用域授权 + 管理 UI(统一 `department_resource_rule` + 引擎组合有效集 + skill/tool/toolset/mcp 四类接入) | 未开始 |

依赖:D 依赖 **C(skill 存储/激活)+ 沙盒底座**;**A 不是 D 的硬依赖**(reviewer P2,消解与 line 24/132 的矛盾)—— D 的典型闭环(上传 docx→artifact→mount→skill)走上传通路、不经 A,只有"backend tool→artifact→skill"(如 DB→CSV→skill)那类场景才依赖 A。C 依赖 B 吗?**不**——披露与 skill 正交(原则 1),C 可在 B 前后任意序;但 B 解决的 30-endpoint 场景是 skill 编排散文的常见消费者,故排在 C 前。E 是 skill 线的 last step(用户:存货预装前先改)。**F 依赖 B**(MCP = 又一个 deferred tool-set provider,B 的 provider 缝 + `search_tools` 是 F 的披露地基),与 C/D/E 的 skill 线正交、可并行。**G 横切**:`department_resource_rule` 表 + 祖先链解析地基随 **C** 落(skill 是首个消费者、可见性必需),tool/toolset/mcp 接入随 B/F 各自 wire-in,**管理 UI + 引擎有效集组合 + 四类齐活 = G**;故 G 依赖 C(地基)+ B/F(资源类型存在)。A、B 各自独立可先做。

## 目标与范围

给系统增加 **标准 Agent Skills**(场景级 prompt 修饰器 + 沙盒可执行 bundle)、**工具渐进式披露**(让 30-endpoint 平台不再 30 份描述常驻上下文)、**MCP client**(把 MCP server 作为又一个 deferred tool-set provider 接入)与 **部门作用域授权 + 管理 UI**(admin 按部门配 skill/tool/tool-set/mcp,一套统一机制),让社区 skill 尽可能「拿来就用」、内网气隙下离线分发,打通对接只说 MCP 的内网系统的通路,并支持按组织部门分发能力。

**Non-goals(本期明确不做)**:
- **联网 skill registry / market**(skills.sh/skild.sh 等)—— 气隙网不可达,market 降维成 `public`+`default_enabled=false` + 链接动作,全 DB/离线(见决策 1)。
- **`context:fork` ad-hoc 子 agent** —— 我方 subagent 是预定义 agent 非 ad-hoc fork,且「只 lead 需要 skill」;v0 标为不支持(skill-creator 那类 fork 存货改造时降级)。
- **substitution 全集**(`$ARGUMENTS`/`` !`cmd` ``/`${SESSION_ID}`)—— 存货零使用,v0 可只支持 `$ARGUMENTS` 子集或全不做。
- **`paths:` 条件 skill**(touch 文件 glob 激活)—— 我方无文件系统语义(artifact 是句柄非路径),v0 不做。
- **`model:` / `effort:` 模型覆盖** —— 这俩是 CC 私有扩展(不在标准 6 字段)、改的是"激活时切调用模型/思考强度"。**不做**:① 它是状态变更型激活,撞原则 8(`read_skill` 纯读、无副作用);② agent 已拥有自己的模型(MD frontmatter,operator 设定),skill 覆盖 = 越权打架;③ 气隙网模型就几个、lead 本就够强、存货零使用,价值近零。
- **server 端 `tool_reference` beta** —— 我方跑任意 backend(qwen via litellm)+ 自有 XML 工具格式,披露走**纯 prompt 级模拟**(见 B)。
- **联网 MCP registry / 公网 MCP 生态自动发现** —— MCP **client 本身现在在范围内(F 阶段)**,但气隙网够不着公网 server;MCP server 由 operator 显式配置(同自定义工具),不做联网市场/自动发现。
- **skill 版本解析 / per-skill venv** —— 依赖只加不 re-pin(继承沙盒原则 7 护栏)。
- **用户身份/凭证透传给工具(B1 用户字段 + B2 OAuth 金库)** —— 与"能不能用工具"(本 plan 做的部门授权)正交的另一根轴:让工具出站请求带用户身份。B1(注入用户已有字段 `department_id`/`role` 到 HTTP 工具模板)+ B2(per-user OAuth token 金库,act-on-behalf-of)推迟到独立 plan。**红线已记**:即便将来做,**只对受信 backend 工具开,沙盒工具一律拿不到用户凭证**(沙盒原则 7 出网纪律)。
- **运行时可编辑 agent 定义(DB+UI)** —— agent 仍是 config(`agents are data`不破)。部门"定制 agent"靠作用域涌现(部门 grant 的 tool/toolset + 部门可见 skill,决策 10),**不克隆 agent**。仅当部门需要**不同 prompt/model**(非工具)时才需要可编辑 agent,届时单独 plan(权限编辑面需 audit)。

## 贯穿原则

1. **渐进式披露归工具层,不归 skill —— 两者正交。** (参考实现实证:CC `SkillTool` 零引用 deferral 机制。)披露**机制** = tool-set(分组/披露单元)+ `search_tools`(发现工具);skill = **场景覆盖层**(注入 body 散文 + 权限覆盖),可**引用** tool-set 但不拥有披露。MCP 将来 = 又一个 deferred tool-set **provider**,进同一 registry、走同一 `search_tools`。把「30-endpoint 披露」这个职责钉死在工具层,skill 保持纯覆盖,是本 plan 的架构脊柱。
2. **Skill = 受信 prompt 修饰器 + 不可信 bundle 的二分,执行全归沙盒。** body/frontmatter 是**受信文本**(注入上下文、改权限);scripts/assets 是**不可信代码/数据**(只在 `--network=none` 沙盒里跑,绝不在 backend 执行)。skill bundle 是新的一类不可信输入,沿用沙盒不可信纪律(选品/审核门禁 = E 阶段)。这也是「先落地沙盒才做 skill」的根本原因 —— 沙盒是 skill 执行的底座。
3. **标准对齐优先于自造;body 原样搬,绝不静默改写。** 采 agentskills.io 开放标准(6 字段、文件夹=单元)。三块各自处理:**body = 自然语言、格式无关**,模型读完用 system prompt 教的 XML 格式发起调用,原样搬;**frontmatter = 映射层**(只 `allowed-tools`→权限模型;CC 私有扩展 `model`/`effort`/`context`/`paths` 全不支持 —— 见 Non-goals,撞原则 8);**substitution = 选择性实现**(只 `$ARGUMENTS` 子集)。社区 skill 的工具词表耦合(`Read`/`Grep`/`Edit` 等)在沙盒里多自然消解(= `cat`/`grep`/`sed`),残留硬耦合靠 **import lint 标记 + 人手改,绝不静默 rewrite**(脆且错改比标记更坏;verify agent 只标记/评估、不改)。
4. **依赖 ≠ 数据,沿用沙盒原则 7 的三层离线投递。** artifact 是用户拥有的**数据**(mount-in/persist、blob 进 DB);依赖是**执行环境**(① 镜像烤通用栈 / ② 离线 wheel bundle 固定位 / ③ skill 自带 asset)。②③同一套 `pip install --no-index --find-links` 机制、不同生命周期(常驻 vs 随 skill),别造两套。**护栏**:skill bundle 只做加法、不 re-pin 基础栈版本(否则一 turn 多 skill 版本冲突逼出版本解析机器,合「fix 复杂度超 feature 价值即退回 scope」)。标准的 `compatibility` 字段 = 声明层,导入时据此校验「需要的镜像没有且 asset 没带」→ 标记/拒。
5. **三态存储,config-种子与 DB-可变二分。** skill 不是 artifact(artifact session-scoped;skill 用户/系统-scoped 跨所有会话)。`preinstalled` = `config/skills/` 启动种子进 DB(运行时不可变,仿 agent/tool「config 即真相、运行时只读」哲学);`marketplace`/`private` = DB 拥有(可变,admin/user 经 UI 改)。skill 非 session-scoped,preinstalled bundle 一份共享(非每会话复制)。
6. **离线 ZIP-of-folder 是唯一分发形态。** 社区四种分发(git clone / plugin install / CLI 包管 / ZIP 上传)里,只有 ZIP-of-folder 不假设 registry 连接,且是 Claude.ai/API/open-skills 共同接受的事实格式 —— 选它做导入导出单元。联网 CLI 全出局。**依赖才是气隙真地雷,非分发**:文件夹随 ZIP 走没问题,运行时 `pip install` 联网约定破在离线 —— 故原则 4 的预置/自带是硬要求。
7. **唯一硬上限是 bundle 大小,复用现成上传挡板;不设调用前 token/索引预算。** 私有化部署**无本地 tokenizer**,全系统 token 判断都依赖推理框架在 LLM call 返回里带的 usage —— 是**事后量**,任何「调用前算预算、超了即拦」的闸落不了地。故:**① token 预算不设** —— skill body 注入只是又一批进上下文的内容,溢出由现有 compaction 路径反应式吸收(compaction 本就拿返回 usage 比 `COMPACTION_TOKEN_THRESHOLD`、事后触发),无独立的 skill token 闸。**② L1 索引预算不设** —— 按预算丢 skill 就是 silent eviction、反违背 loud-fail;索引大小靠 **operator 策展**约束(预装有限 + 上传有限),真臃肿了答案是把披露机制套到 skill 上(变可搜索而非全常驻,属 B/C 阶段),不是运行时预算检查。**③ bundle 大小是唯一能在上传期按字节事前判定、不需 tokenizer 的上限,且按信任分层而非 flat 值**:上限是**配额防滥用闸、不是正确性闸**,判别「绕过谁受伤」—— **用户传 private skill** 走用户上传路由、**现有 per-user blob 配额 + 413 照旧**(滥用向量,大依赖包本不该让普通用户随手传);**admin 策展 skill 无配额闸**(preinstalled 走 `config/skills/` 启动种子、根本不经上传路由,marketplace admin 上传路由对 per-user 配额豁免 —— skill 是 shared/系统拥有、不记某用户头上,且合法携带离线 wheel)。loud-fail + config 隐藏常量(合「Minimize tool parameter surface」,不做模型可调参数)。
8. **「激活」是轻量动作:能力持有复用 `always_allowed_tools` 的 path 持久化模式,不扫历史、不建状态机、不豁免 per-turn ephemeral 纪律。** 两个参考实现都印证激活 = 把正文弄进 context 的 tool_result(opencode `skill` 返回 `<skill_content>`;CC 注入隐藏 message),唯一副作用是改 permission。两个动作:① **L2 body 进 context**(复用 read 的 tool_result,随压缩自然进出);② **skill 能力进有效集**。**两条权限轴必须分清**(`engine.py:843`,详见决策 11):**轴 A = 等级 `{auto, confirm}`**,唯一来源工具定义,config/agent MD/skill 皆不可改;**轴 B = 成员 `{enabled, disabled}`**,agent MD 写 / m2m 减 / skill **enable-only**。**skill 只动轴 B**(在 agent MD 宇宙内翻开 `disabled`),**永不碰等级** —— 授 `Bash` ≠ 自动批准,`Bash` 的 `confirm` 照样弹;**等级唯一运行时变更 = 用户「始终允许」`confirm→auto`**(`always_allowed_tools`,降级唯一路径)。**能力持有 = 新 `state["active_skills"]` slug list,照抄 `always_allowed_tools` 生命周期**(回合内 `state`、回合末写 `Message.metadata`、下回合父消息捞回 —— `controller.py:176`/`529`):O(1) 判断,**不扫 `EventHistory`**(那是重建 LLM 上下文用的、非权限热路径)。这比「同源派生」更对 —— body 随压缩走(模型忘了指令再 `read_skill` 一次),但**能力轴不被压缩静默撤权**,两件事独立解耦。v0 = append-only sticky(读了就对话内常驻可用,同 `always_allowed_tools`),显式停用 defer。**关键护栏**:`active_skills` **只管能力轴、不复活 L3 mount** —— 沙盒 per-turn 销毁,带能力跨 turn ≠ 带 mount 跨 turn,下回合仍须自己 mount;沙盒 plan 已为「跨轮 mount 一致性」打过仗并收手(变更日志 2026-06-12:提示词压不住历史伪证、loud-fail 自纠才是真机制)—— **绝不让能力持有把这套被否决的机器偷渡回来**(合「fix 复杂度超 feature 价值即退回 scope」)。

## 已锁定的决策

1. **Skill 可见性 = `visibility`+`default_enabled` 两正交字段(替不透明 scope)+ 稀疏 `user_skill` 覆盖;部门作用域走统一 `department_resource_rule`(决策 10)。** Skill 表带:`owner_user_id`(private 用、shared 为 null)+ `visibility`(`private` 仅 owner / `public` 全员 / `department` 按 grant)+ `default_enabled`(shared skill 默认是否注入)。**原三 scope 拆成这俩正交字段**:preinstalled = `public`+`default_enabled=true`(config 种子);marketplace = `public`+`default_enabled=false`(目录可见、opt-in);**market = link 动作全 DB/离线,无联网 registry**。**per-user 覆盖 = 稀疏 `user_skill(user_id, skill_id, enabled)`**:无行=走 visibility/default,有行=用户显式开关 —— **marketplace 选用=enabled 行、关掉预装=disabled 行,link 与 toggle 是同一机制**(不是两套)。部门可见(`visibility=department`)= 用户祖先链 ∩ `department_resource_rule`(决策 10)。**`visibility` 现升为 skill/tool-set/mcp 三类共享字段**(原只 skill 有):它定每个单元的默认姿态(`public`=默认 allow / `department`=默认 deny),决策 10 的部门规则据此判一行是加还是减 —— tool-set/mcp 的 `visibility` 写在各自 config frontmatter(决策 10/Phase B/F)。`private` 仅 skill 有(owner-only)、**不进 `department_resource_rule`**(决策 10)。
2. **披露 = tool-set 文件格式 + `search_tools` 内建工具,纯 prompt 级。** 新增「一文件多 tool」的 tool-set 格式(多 endpoint 平台、未来 openapi 生成脚本的落点),整组标 deferred:索引行常驻 `<available_tools>`、schema 不渲染;模型调 `search_tools`(`select:Name,Name` 或关键词)拿回完整 XML 工具描述作 tool_result。**返回值复用 `generate_tool_instruction`**(= 渲染系统提示词可用工具那套);**描述随 tool_result 留在历史、后续轮模型自然可见 —— 不维护「已发现集」、不重渲染进 system prompt**(比 CC `extractDiscoveredToolNames` 追踪更简,且无 server `tool_reference` 依赖)。**为何这里靠历史、`active_skills` 却要 metadata-state(决策 11/原则 8)**:发现是**上下文**问题(描述在不在 context),非**权限**问题(可调与否由 resolver 按 unit 成员判、**与发现无关** —— deferred 只是描述没渲染、工具在授权 unit 里闸照过);上下文的家是历史,被压缩则模型见常驻索引行**自己再 search 一次**(loud-fail 自纠),无需 durable state。**附带**:发现动作不改写 system prompt —— 索引/catalog 已挪动态 reminder、只 grammar 留前缀(决策 11/Phase B line 103),前缀稳定可缓存、不被发现动作击穿。
3. **存储 = 消费列 + `metadata` JSON 杂项列 + `skill_md` 全文列 + 完整原始 blob(含 SKILL.md),复用 blob 存储。6 标准字段全部 DB 落位、按"消费与否"分流。** 四处:① **消费列**(`name`/`description`/`allowed_tools`/`compatibility`/`slug`/`visibility`/`default_enabled`/`owner_user_id`,**`visibility`+`default_enabled` 替原 `scope`,见决策 1**)—— 系统要查询/消费的字段反规范化出来(`compatibility` 供气隙依赖校验,决策 6),L1 批量列举 + 权限 + 校验,**不解 blob**;② **`metadata` JSON 列** —— frontmatter 里**系统不单独消费的字段全归这**(`license` + 标准 `metadata` 容器[含 `version`] + 任何未知扩展),"用不上的扔 metadata"、`license` 不开独立列,免解 blob 的杂项读取层;③ **`skill_md` TEXT 列** —— SKILL.md 全文,L2 `read_skill` 直接返回(免解 blob);④ **`bundle` BLOB** —— **完整原始 zip(含 SKILL.md + references + scripts + assets)**,L3 mount + 导出。**关键:blob 是真相源、存验证通过的原始上传整包**,①②③ 都只是反规范化的查询/读取副本(**不碰原 frontmatter 结构** —— `license` 在原 SKILL.md 顶层就还在顶层,DB `metadata` 列只是副本归类、非挪字段);故**导出无损仍是 construction 保证的**(直接吐原 blob,不从列重序列化 → 未知字段/格式不丢);代价是 SKILL.md 三处冗余(小文本,换无损+简单导入,值)。`preinstalled` 同 bundle 全用户共享一份(非 session-scoped)。
4. **三级渐进式披露,「激活」= 一次普通 read + 一次普通 mount,无独立机制。判别线:SKILL.md 走读通路,bundle 里任何东西走沙盒。** L1 =「有哪些 skill」由 **ContextManager 注入** `<available_skills>`(name + description,仿 artifact inventory 先例 —— 列表是 ContextManager 职责非工具职责,故**不学 opencode/CC 把 L1 塞进工具描述**)。L2 = 模型调 **独立 `read_skill(slug)` 工具**返回 `skill_md` 全文作 tool_result(纯文本、provider-agnostic,opencode 实证此为可移植形态;**死简单——按 slug 返回 skill_md,无 path 参数、后端不解 zip**;**不合并进 `read_artifact`** —— 身份空间不同[user-scoped slug vs session-scoped id]、store/Manager 不同,合并要么加 type 参数[违 legibility]要么藏第二个 Manager;**镜像 read_artifact 契约**:句柄进/内容出、`max_result_size_chars=inf` 永不二次 persist、`AUTO`)。正文之外的一切(references/scripts/assets)**不走读通路、一律去沙盒读**(`read_skill` 输出附「其余细节含 references 须 mount 进沙盒读」提示)。L3 = 模型按需调 **现有沙盒 `mount` 工具**(复用,非新机制)把完整 bundle 挂进沙盒**固定约定路径** `/workspace/.skills/<slug>/`,模型用 bash `cat`/`python` 读跑;`${SKILL_DIR}` 因路径由 slug 确定**在 mount 前即可解**(替成约定路径),mount 返回真实路径作运行时确认 —— **占位符问题就此关掉**。
5. **bundle 走模型驱动 per-turn mount(复用现有 `mount`),不 auto-mount、不维持跨轮一致性。** 激活**无沙盒副作用**;`read_skill` 是纯读。bundle 内容(references/scripts)在哪轮用就哪轮 mount(同 artifact mount),受沙盒既有 per-turn ephemeral 纪律 + `<sandbox_status>` + bash file-not-found loud-fail 自纠管 —— **绝不为 skill 重建跨轮 mount 一致性机器**(沙盒 plan 已打过这仗并收手,见原则 8)。persist 仍模型驱动(沙盒 `persist`→artifact 现成)。
6. **依赖 = `compatibility` 声明 + 镜像通用栈 + asset 离线 wheel。** 通用重栈烤镜像(python + lxml/openpyxl/pypdf/pdfplumber/pandas/Pillow + libreoffice/poppler/pandoc/qpdf,存货与调研同指一组);长尾走 asset `wheels/`,**模型在沙盒里按需** `pip install --no-index --find-links` 离线装(**非激活自动**,守原则 8;详见 Phase D + changelog 06-18);import validator 据 `compatibility` 交叉校验(镜像栈 ∪ 自带 wheel),缺口标记/拒。
7. **导入 = 单管线两道门:`upload → unzip → 硬门槛(确定性 validator,阻塞)→ 软门槛(verify agent,可强制通过)→ 解析 frontmatter + 入库`。** **硬门槛** = plain code 阻塞门禁(便宜确定、先跑快速失败),借 `skills-ref`/`skill-validator` 规则:6 字段 schema、name↔dir、孤儿文件、未闭合 fence、链接解析、SKILL.md 体量(legibility 警告)、`compatibility`-vs-环境。**bundle 字节上限不在此** —— 那是上传路由的配额闸、按信任分层(原则 7③,admin 无闸)。**软门槛** = verify agent(懂本系统约束的 agent,贵=LLM 调用、后跑、**可 override**):**职责单一 = 评估「这个 skill 能不能在本系统跑」**,就三查 —— ① 用了没有的工具/harness?② 需要装依赖(`compatibility` 声明 vs 镜像栈 ∪ wheel)?③ asset 放好没?**输出诊断(能跑/缺什么),不改 body、不产 rewrite 或改造 diff**(改造/简化是人的事,见决策 8)。**用户私有上传时可选/可跳过**(LLM 成本)。「lint 不静默改写」在此:残留硬耦合由确定性 lint 标记 + **人**审手改,运行时禁静默 rewrite。入库 = 索引列 + `skill_md` + 完整原始 blob(决策 3)。
8. **预装 pandoc-first(瘦 bundle)。** 存货 4 文档 skill 三份重复 `office/` + 40 xsd 做 OOXML 手术;常见 Word 路径(读/转/简单生成)预装 skill **首选 pandoc/libreoffice**(镜像内,一条 CLI),OOXML 拆解只留给 pandoc 真做不到的(改痕/批注/精确版式)。删常见路径的三份 `office/` bundle。**这个分析简化是「我们」在 E 阶段人工做的工程动作**(预装集就 4 个、值得手工调),**不交给 verify agent**(后者只评估能不能跑、不改 skill,见决策 7)。
9. **采开放标准 6 字段,DB 全部落位、按消费分流。** `name`/`description`/`license`/`compatibility`/`metadata`/`allowed-tools`;`version` 归 `metadata`。**6 字段在 DB 都有落位(非只 name+description),按"系统消费与否"分流**(决策 3):消费的开独立列(`name`/`description`/`allowed_tools` 必做,`compatibility` 供气隙校验),**系统用不上的(`license`/`metadata`/`version`/未知扩展)全归 `metadata` JSON 杂项列**;真相源仍是原始 blob(**不改 frontmatter 结构、导出无损**)。**硬依赖只 `name`+`description`**(存货现状、其余常空),其余字段消费宽容缺失。`allowed-tools` 标准里本就 Experimental → 映射到我方 per-tool 权限模型(决策 11 轴 B)、不欠生态硬兼容。
10. **部门作用域授权 = 一张统一 polymorphic `department_resource_rule` 表,服务 skill/tool/tool-set/mcp 四类;agent MD 不动、引擎运行时组合。** `department_resource_rule(department_id, resource_type, resource_id, effect)`,`resource_type ∈ {skill, tool, toolset, mcp}`、`effect ∈ {grant, deny}`,一个资源授多部门=多行,按 **name/slug** 引用。**为什么一张 polymorphic 表(非四张带 FK)是对的而非将就**:四类资源**异构**——只有 skill 是 DB 行,**tool/tool-set 是 config(`config/tools/*.md`)、按 name 标识、根本没有表可 FK**,mcp 是 operator 配置同理;polymorphic 丢 FK 完整性在此可控。**软引用同步(无 DB CASCADE,删资源不自动删 grant 行)**:功能正确性**始终靠解析 `∩ 已加载` 兜底** —— 悬空 grant(目标已删)自然空转无害、各容器拿自己 registry 各自正确、HA 安全、不依赖删除。清理/防 ABA(name 复用嫁接旧 grant)按真相源分:**skill(DB 实体、高频增删)= 应用侧 cascade**(删 skill 的 Manager 同事务删其 grant,DB 单一真相 → 全局一致);**config 类(operator 手动维护、低频)= 系统不自动碰 m2m**,operator 改 config 时自己同步 grant(运维契约,合「config 即真相、operator 显式配」;其 registry 真相源=各容器内存、滚动更新窗口无单一真相,自动判断必抖动,不值得为已无害的悬空造 HA 一致机器)。admin m2m UI 孤儿提示只对 skill 类(DB 真相可靠)。**到时再敲定**(同步):skill grant 是否改存不可复用 id 彻底根治 ABA;grant 校验 registry 存在的接口。详细演进(reconcile→实时算→运维契约三轮)见 changelog 06-18。**判定** = 用户 `department_id` 的 `parent_id` 祖先链 ∩ 规则集(指父覆盖整子树;命中后方向查单元 `visibility`)。**组合 = ceiling + 按单元 `visibility` 的 grant/deny 例外**(等级/成员两轴解析详见决策 11):`agent MD` 是该 agent 的**最大工具宇宙**(`config 即真相`不破);部门规则 + skill 都只在宇宙内动、**不能引入宇宙外工具、不碰等级**。**关键:一行的方向 = 行里显式 `effect ∈ {grant, deny}` 列**(**非从 `visibility` 派生**),`visibility` 退化成**"未列出部门的默认值"**(`public`→默认 allow、`department`→默认 deny;`public` 下 deny 行有意义、grant 行冗余,`department` 反之)。一行 = "该部门子树对此单元显式 grant/deny",未列出部门走 visibility 默认。**为何显式而非派生(reviewer P1)**:direction 是派生态时,operator 改 config `visibility`(`public`→`department`)会把同一 **deny 行静默翻成 grant、把本想禁的部门反授权** —— 且 config 类 visibility 在 config 文件、无 Manager hook、无旧值可比,DB-only 的阻断挡不住。**存 `effect` 后改 visibility 只动默认值、永不翻已有行**(`public`→`department` 时旧 deny 行变冗余、谁都拿不到=过度收紧的安全方向),DB/config 一视同仁、无须 hook/mirror/snapshot。UI 录入按 visibility 预填 effect(`department` 新行默认 grant、`public` 默认 deny),operator 不用想,但**存显式值**抗 visibility 漂移。**故 tool-set/mcp 也要补 `visibility` 字段**(原只 skill 有 —— 没这字段 resolver 无从区分加减;散装 builtin 无单元承载、恒 `public`,要做部门专有就编进一个 tool-set)。两方向共用同一套祖先链树解析、**各自只需 1 行**(树让"二级部门一刀覆盖整子树"成立 → 加减都不必枚举全树)。default 姿态长在资源上、不在表名,故表中性命名 `department_resource_rule`(原 `department_grant`,grant 偏向已不准)。**Manager 层不变量**:**`private` 资源不进本表**——private=owner-only、无部门语义,Manager/UI 硬拒写入(默认/方向只对 `public`/`department` 定义)。(上轮"改 visibility 阻断"已被 `effect` 显式列取代:翻转按构造消失、无须阻断;改 visibility 后旧行只是变冗余 dead row、无害,连同删资源的孤儿行一起归下述 **check 端点**扫,见 G。)**这套是横切复用机制,非 skill 专属**:解了"静态 agent 白名单被 skill/MCP/部门作用域三方压垮"那条横切线(挂 memory `permission-param-granularity-direction`)。

11. **工具权限 = 两条正交轴,经统一 `EffectiveToolset` resolver 解析、单算多消费。** **轴 A 等级 `{auto, confirm}`**:**唯一来源 = 工具定义**(`tool.permission`,「写啥是啥」),config/agent MD/skill **一律不可改**;**唯一运行时变更 = 用户「始终允许」`confirm→auto`**(现有 `always_allowed_tools`,跨 turn)。**轴 B 成员 `{enabled, disabled}`(+absent)**:agent MD 写、m2m 减、skill enable —— 与等级是**不同枚举、不同轴**(`disabled` 是成员态,**不进 `ToolPermission` 枚举**,后者保持两值)。**四层链,逐层只收窄/翻开**:① **工具定义**(built-in/custom)= 系统实际能跑什么 + 各自等级,雷打不动;② **agent MD** = 该 agent **最大成员集**(每项 enabled / `disabled`;absent=不在宇宙)= 天花板;③ **m2m/部门**(决策 10)= 在 ② 上按规则行显式 `effect` 做 **grant/deny 例外**(visibility 只定未列出部门的默认值,详见决策 10),仍只在宇宙内动;④ **skill**(`active_skills`,原则 8)= **enable-only**,翻开 ② 里的 `disabled`,**不加/删 key、不碰等级**(无 disable 操作 —— skill 语义只有「我还需要这几个」)。**陷阱按构造消失**:全链无人能改等级(除运行时用户),「`confirm`默认→`disabled`→`auto`」洗白不可能。**P0 信任边界(明示,非加机制)**:私有 skill 能翻开的**只有 agent MD 里已声明为 `disabled` 的工具**——做不到引入 `absent` 工具、做不到碰等级。故 operator 的「任何 skill 都别碰」硬门槛 = 写 `absent`(根本不声明),**不是 `disabled`**(后者按设计就是"可被 skill 开",见 Phase B L1 撑爆控制);敏感工具靠 `confirm` 等级(skill 不可改、用户每次在环)兜底,**私有 skill 无法静默提权**。因此 verify 门禁非权限边界,私有上传可跳过 verify 不破此模型。残留(无 per-skill ACL 决定"哪个 skill 能开哪个 `disabled` 工具")窄、留将来,不为它加 skill trust-tier 状态机(合「fix 超 feature 价值即退回 scope」)。**单元(tool-set/mcp)**:成员/减法/enable 都在**单元粒度**(声明/减/enable 整个单元;**成员命名 `<unit>__<tool>`:`__` 是唯一分隔符、恰好出现一次,unit/tool 名只用单 `_`、都不得含 `__`** —— 故 MCP 工具 = `<server>__<tool>`(**不带 `mcp__` 前缀**;否则两个 `__` 让 unit 含 `__`、撑破 `<unit>__*` 通配)。前缀 `<unit>__*` 可整组匹配、前缀授权据此落地;**resolver/config 只按已知 unit 名 prefix 匹配、不靠 split `__` 反推 unit**(reviewer P2,免错误 split),unit 名跨 tool-set/MCP **全局唯一,registry 启动期撞名 loud-fail** 强制,见 B);等级仍 per-tool 来自各自定义(MCP 配置期不知工具 → 用 **server 级默认等级**=MCP 的「定义」)。**tool-set ↔ MCP 同型**——授权(单元粒度减/enable)、披露(`search_tools` 一条道、unit 非 discovery 边界、两层够,见 B)、命名(`<unit>__<tool>`,MCP=`<server>__<tool>`)整链对齐,唯一别 = **静态(config 时已知)vs 动态(连接时灌注册表)**;故 B 把缝按 MCP 形状留对,F 接入是纯加法。**存储**:agent MD `tools:` 存**单元引用**(tool 名/tool-set 名/mcp server 名 + 成员态),**不摊平**;喂引擎的扁平 `{tool: level}` 由 **resolver 解析时展开**(tool-set 静态、MCP 运行时发现后),每项套「单元成员性 + 该工具定义等级」。**resolver = 唯一解析点,所有读点读同一输出**:渲染(`ctx:89`)、条件提示词段(`ctx:204/215`,skill 开沙盒工具其指令段须随之出现)、执行闸(`engine:759`)、等级(`engine:844` 塌成就 `tool.permission`)、`search_tools` 可见集过滤(B 新建,reviewer P1)—— 由构造消灭多消费点漂移。**迁移≈零**:现有 agent MD `{name: auto/confirm}` 一律按 enabled 读(等级值变装饰、实际来自工具定义,且当前所有值本就等于默认),只在想要「关但可被 skill 开」处写 `disabled`。**成员态 schema(开工定)**:旧 `auto`/`confirm` → enabled,`disabled` 是**唯一**禁用字面量,其余未知值启动期 **loud-fail**(不静默当 enabled,免拼写错误悄悄放行)。

## 阶段

### A — 工具结果→富格式 artifact(引擎前置,沙盒数据入境的受信通路)

**做什么**:让 backend 工具(受信/有网/有凭证,如 HTTP 工具 GET 回 CSV)能把结果存成**具名、带 content_type、可二进制**的 artifact —— 成为 `create_from_upload` 的**第三调用方**(前两个:用户上传走 engine staging、沙盒 `persist`)。首个消费者 = `web_fetch` 文件旁路(见下)。这是「backend 工具 → artifact → mount → 沙盒 → persist」受信数据脊柱的入境第一环,补 `tool-result-artifact-mount.md` 先例的「只能溢出兜底(>50k、固定 text/plain、不能具名)」缺口。

**包含**:
- **声明式而非命令式**:`ToolResult` 加可选 `artifact: Optional[ArtifactSpec]`(`title`/建议 id、`content_type`、文本或 blob)。工具**声明**「把我的 data 存成这个 artifact」,**不**持 `ArtifactService` 句柄(守三层模型:通用工具保持哑,只有内建 artifact/sandbox 工具——它们本就是 manager 层——直接碰 service)。
- **引擎路由**:`_maybe_persist_tool_result`(`engine.py:667`)加分支:`result.artifact` 命中 → `ArtifactService.ingest_tool_result(...)`(具名、带类型、blob 可、配额闸);现有溢出路径降为**同函数的无名兜底**。两者共用 `create_from_upload` 的配额/blob/去重内核。
- **模型见句柄非 blob**:tool_result 变预览片 + `read_artifact(id=)`/`mount` 提示(同今溢出)。
- **web_fetch 文件旁路(首个数据工具)**:现有 `web_fetch` 走 jina 提网页正文、对 PDF/二进制 URL 本就坏 → 加旁路:**按 URL 尾缀自决**,文件类(`.pdf` 等)→ 直连下载(绕 jina)置 `ToolResult.artifact`(blob + content_type 读响应头、尾缀兜底,PDF→`application/pdf`),网页类照旧。**内建工具运行时自决、非模型参数**(合「Minimize tool parameter surface」:模型只调 `web_fetch(url)`,persist 藏工具内)= line 88 声明式的另一合法形态(operator MD 静态声明留给未来真有的 config/tools API 工具)。补真实缺口 + 端到端串 A→mount→D(pdf skill 输入路径);**只有网部署有效**(内网 web 工具禁用,intranet 入境靠沙盒 persist)。**路由判断用 URL 尾缀**(零成本、绕开 Content-Type 探测的卡顿——现有 `_detect_content_type` 已是此取向,`web_fetch.py:168`);`artifact` 的 `content_type` 顺手读下载响应头、尾缀兜底(区分:为路由提前探测 Content-Type=要额外 HEAD/请求 可能卡,已在下载时顺手读 header=免费)。旁路插在 Jina 之前分流(文件类不进 jina),复用 `_fetch_pdf` 的直连下载 + `_read_capped` 封顶,「抽文本」换成「存 blob」;文件按 bytes、绝不 `.text`(避编码探测)。**SSRF 必须自带**(reviewer P1,代码核实):现有直连前 `validate_public_url` 在 `_fetch_single_url:202`、**仅在 Jina 失败后才跑**,旁路插在 Jina **之前**会绕过它(`_fetch_pdf:367` 本身不校验、靠调用方兜)→ **旁路直连下载前必须自己调一次 `validate_public_url`**,并保 `allow_redirects=False`(`:371` 已有)。注:PDF 行为从「抽文本」变「blob 句柄」(对齐上传 blob-only),可选附文本预览兼顾简单读。

**到时再敲定**:`ArtifactSpec` 精确字段;具名 id 与现 `_normalize_filename_to_id`/去重的衔接;`source="tool"` 的配额归属(同上传走 per-user blob 配额);HTTP 工具 content_type 派生的边界(响应头缺失/撒谎时)。**验收项**:旁路 SSRF —— 每次直连前再校验、`allow_redirects=False`、覆盖 302→内网/元数据地址(`169.254.169.254` 等)被拦的测试。

### B — 工具渐进式披露(tool-set 文件格式 + `search_tools`;MCP 适配缝)

**做什么**:解决「30-endpoint 平台 = 30 份描述常驻 system prompt」。披露单位 = **场景/平台(tool-set)而非单工具**,机制 = deferred 索引 + 按需加载。**与 skill 正交**(原则 1)。

**包含**:
- **tool-set 文件格式 + 命名空间**:`config/tools/` 新增「一文件多 tool」格式(YAML 定义一个平台的 N 个 endpoint + 一条 set 级描述/索引行)。**成员工具命名 `<setname>__<tool>`(`__` 唯一分隔符、恰好一次;MCP 同型 = `<server>__<tool>`,**不带 `mcp__` 前缀**)**,loader 据 set 名自动加前缀(作者写裸 tool 名、零负担);**`__`=分隔、单 `_`=名内,unit 名与 tool 名都不得含 `__`**,故 `read_artifact` 这类扁平 builtin 仍可区分、且 `<unit>__*` 通配无歧义。**为何要命名空间(不只防撞名)**:① **关键——让 unit 粒度授权/减法按前缀 `<setname>__*` 工作,与 MCP `<server>__*` 同构**(决策 11「单元粒度」+ Phase F 靠此落地;不带前缀,resolver 没法把一个 set 整组拎出)。**resolver/config 只按已知 unit 名 prefix 匹配、不 split `__` 反推 unit**(reviewer P2:`__` 仅一次本可 split,但靠已知 unit 名更稳、防 tool 名意外含分隔);**unit 名跨 tool-set/MCP 全局唯一、registry 启动期撞名 loud-fail**(`github` tool-set 与 `github` MCP server 不可并存);② 跨 set 同名 endpoint(`create`/`search`)不撞;③ 模型可读(知道哪个平台)。**用 `__` 不用 `:`**:工具名虽是 `<name>` 文本内容(happy path 冒号尚可),但**参数是 XML 标签 + `_repair_tool_name_as_tag` 修复路径会把名当标签**(`xml_parser.py`),冒号撞 XML 命名空间崩;`__` 标识符处处安全。agent MD 存 set 名(unit ref,决策 11)、resolver 展开成 `<setname>__*` 成员。这也是未来 `scripts/` 级 openapi→tool-set 生成脚本的落点。tool 定义粒度不变、权限模型不动,只是分组+授权单元变成 set。
- **`EffectiveToolset` resolver 骨架在此立(基础设施,决策 11)**:resolver 的价值 = 把现散在 4 处**各自直读 agent MD dict** 的读点——渲染(`ctx:89`)、条件提示词段(`ctx:204/215`)、执行闸(`engine:759`)、等级(`engine:844`)——收成**唯一解析点**,全读点读同一输出。**这个收口必须在 B 做、不拖到 C/G**:B 是第一个改变工具集形状的阶段(tool-set 单元 + deferred),收口越晚,同一组读点被反复 refactor 越多次(B 改渲染、C 加 `active_skills`、G 加 dept 规则 = 同 4 点碰三轮 = 「同形改动反复 → 退回架构」信号)。**B 新建的 `search_tools` 从一开始就读 resolver**(可见集过滤,line 105)= 第 5 个消费点,但它是新建、非历史返工对象。**B 立骨架时输入只有静态两样**:① agent MD 成员(enabled/`disabled`)② tool-set 单元展开(`<set>__*`,见上条;tool-set 工具要可调本就必须展开 —— 这是 resolver 的 B-native 职责、**非 G 泛化项**)。输出 = 扁平 `{tool: level}`(仅 ON、等级塌成 `tool.permission`),deferred 渲染只改这一个 resolver 的渲染读点、闸/级不变。**之后每阶段只「加一个输入层」、不再碰读点**:C 加 `active_skills`、F 加 MCP 运行时单元填充、G 加 dept 规则(按 visibility grant/deny)—— 由构造消灭多消费点漂移(line 167 原把骨架放 C,提前到 B)。
- **deferred 渲染(走 CC 激进路线)**:tool-set(或单工具标 `defer: true`)只在 `<available_tools>` 出索引行,完整 XML 描述(含 `parameters`)不渲染。**索引行 = tool-set 一条 set 级描述 + 成员工具名列表(光名字、不给每工具 desc);单工具 deferred = 光工具名**(对齐 CC —— CC 连描述都不给、赌名字自解释;我方 tool-set 名字外包一层 **set desc 做语境**,比 CC 孤立名字更稳)。完整描述由 `search_tools` 经 `generate_tool_instruction` 按需补全。**defer 是显式开关、不按 token 自动**:config 配了 `defer: true` 才 defer(只出 name)、没配则完整描述照常注入;私有化无 tokenizer 算不了预算,故**不学 CC `tst-auto` 的 token 预算自动 defer**(合原则 7、一切 operator 显式配)。
- **tool list 挪进动态 reminder(prompt-caching)**:工具描述现在在 `system_parts`(`context_manager.py:92` `generate_tool_instruction`)= system prompt 前缀;skill enable / dept 减法让工具列表**会话内动态**后,留前缀会在每次变化时打掉整段历史 prompt cache。**改:拆开 `generate_tool_instruction` 两段**(reviewer 提:别把稳定 grammar 一起降权)—— 稳定的 tool-call **协议语法**(`xml_formatter.py:17-42` 的 `<format>` 块,会话无关、不变)**留 system prompt 前缀保可缓存**;只把**动态的工具 catalog/索引**(逐工具描述,`:44` 循环)挪进 `_build_dynamic_context`、与 `artifacts_inventory`(`:209`)同级(历史末尾 `<system-reminder>`;`context_manager.py:77` 注释早立此原则=动态的放 reminder、别坐前缀,artifact inventory 已这么放,工具 catalog 是唯一没跟上的)。catalog 变化只失效末尾、grammar 前缀恒稳;免去「baseline 留前缀 / 动态挪历史」的分层(enable 怎么变都只动末尾)。**reminder 纪律**:有序——`task_plan` 永远打头锚当前 task,tool list 进资源清单区(挨 artifact inventory)、保持 deferred 索引体量(否则淹没 task)。**当前生产无 APC=收益暂 0 但无害**(6/11 关),未来 APC 重开/换部署自动受益 + legibility 更近;因 prompt 全可控(XML 级、非原生 `tools` 参数)故零成本可挪。
- **单元非 discovery 边界(两层够、无第三层)**:MCP `tools/list` 协议层就**扁平一发返全部**(name+desc+JSON-Schema),无原生渐进发现;tool-set 同理。故 unit(tool-set/mcp)只做**授权 + 生命周期边界、非 discovery 边界**——披露就两层:**① 检索注册表(`search_tools`)= 全成员扁平注册**(MCP 连接时灌、tool-set 启动时灌;100 个也是检索去扛,那是它本职)、**② L1 索引 = enabled 工具 name 全注入**(tool-set/unit = 一行 set desc + 成员光名字,CC 激进路线 line 101;**不做按大小自动展开/折叠**)。怕大 unit(50 工具 MCP)撑爆 L1 → 不靠折叠机制,靠 **agent MD 把该 unit 配 `disabled`**(不进 baseline、不注入),由 **skill 按需 enable** 带进来(决策 11 轴 B,激活时进 reminder 历史层)—— L1 注入量交 operator 显式控制(`config 即真相`),系统不做智能折叠、无新机制。「这个 server 里有啥」= `search_tools(unit=x)` 普通检索查询,**不做专门 `list_members` 第三层**(完整 usage 已由 `search_tools` 返回 = 那一层)。tool-set ↔ MCP **同型**,唯一别 = **静态(config 时已知)vs 动态(连接时灌注册表)**。
- **`search_tools` 内建工具**:模型调它(`select:Name,Name` 直选 或关键词搜),返回完整 XML 工具描述作 tool_result(**复用 `generate_tool_instruction`**)。**结果必须过滤到当前 EffectiveToolset 可调集**(reviewer P1):registry 全成员扁平注册只是**存储**,`search_tools` 作为**工具**必须套 resolver 输出 —— 含 enabled-but-deferred(本就该搜得到、这是 defer 的意义),**排除 disabled/absent/已减/未授**;否则泄露不可调工具描述 + 模型反复试不可用工具。故 **`search_tools` 是 resolver 的又一读点**(`search_tools(agent,user,active_skills)→可调集`),反而强化单点解析。描述随 tool_result 留在历史、后续轮自然可见,**不维护已发现集、不重渲染进 system prompt**(被压缩则模型见索引行自己再 search,自纠 —— 详见决策 2)。新内建工具注册进 `dependencies.py:_load_tools()`,改 `generate_tool_instruction` 让 deferred 集渲染成索引而非全描述。**纯 prompt 级**(无 server `tool_reference` 依赖)。
- **provider 抽象(F 的地基,B 建好)**:给 tool 模型加 `source`/`provider` flag(仿 CC `isMcp`);registry 归一化所有来源到一个 tool 形 + 合并函数。这样「MCP server = 又一个 deferred tool-set provider、`<server>__<tool>` 命名(`__` 唯一分隔、无 `mcp__` 前缀,决策 11)、走同一 `search_tools`」在 F 是纯加法。B 须按 MCP 的形状把缝留对(命名空间、按 server 名搜、动态 set),F 才接得干净。
- **部门作用域 wire-in 的边界(消费全留 G)**:tool-set 将来接入 `department_resource_rule`(`resource_type=toolset`),方向由 tool-set 自身 `visibility` 定(`public`→减、`department`→加,决策 10)。**B 只负责两件准备**:① 给 tool-set 加 `visibility` frontmatter 字段、② 让 tool-set 有稳定 unit name 作 `resource_id`。**部门规则的实际消费(查表 + 树解析 + 进 resolver)全在 G**(规则表 C 才建、dept 输入层 G 才加,见 line 101 分阶段输入);**B 的 resolver 骨架输入只有静态两样、不认部门规则**(修正:原写"resolver 减法认 grant"与 line 101 矛盾)。授权 UI 归 G。

**到时再敲定**:tool-set 文件 schema;**两个待显式过的语意**(memory `tool-ecosystem-positioning` 已标):① 历史中 tool_result 引用了「描述已随压缩掉出 context」的工具时的提示文案(自纠靠模型重 search,但要确保索引行点明可重取);② `always_allow` 跨 set 同名工具语意。

### C — Skill 核心(存储 + L1 注入 + `read_skill` 工具 + 权限/上下文覆盖)

**做什么**:落地标准 Agent Skills 的**纯 prompt 修饰器**部分(不含 bundle 执行,那归 D)—— 可见性存储(决策 1)、L1 索引常驻(ContextManager)、L2 `read_skill` 返回 `skill_md`、`allowed-tools`→权限覆盖;**并落 `department_resource_rule` 表 + 祖先链解析地基**(决策 10,skill 是首个消费者,tool/toolset/mcp 接入 + UI 归 G)。**C/D 阶段线 = 有没有 bundle**:单 SKILL.md(无 bundle,如 consolidate-memory)C 完即可用、不碰沙盒;**有 bundle(references 或 scripts)的等 D**(bundle 任何内容都走沙盒读,见决策 4 判别线)。

**包含**:
- **`Skill` 表 + 覆盖表 + grant 地基**(决策 1/3/10):**消费列** `slug`/`owner_user_id`(shared 为 null)/`visibility`(private/public/department)/`default_enabled`/`name`/`description`(`when_to_use` 折进)/`allowed_tools`/`compatibility` + **`metadata` JSON**(`license`/`version`/未知扩展等系统不消费字段,决策 3/9)+ **`skill_md` TEXT**(全文,L2 读)+ **`bundle` BLOB**(完整原始 zip 含 SKILL.md,L3/导出);`user_skill(user_id, skill_id, enabled)` 稀疏覆盖;**`department_resource_rule(department_id, resource_type, resource_id, effect)`**(`effect ∈ {grant,deny}` 显式存、非派生,决策 10;此阶段先建表 + 祖先链解析,skill 用 `resource_type=skill`)。Repo/Manager/Router 三层(skill 非 session-scoped,Manager 做 ownership/可见性/序列化)。
- **API + 前端**:CRUD + 可见性/启用 toggle 端点;前端 = 设置/管理页(非对话流内),admin 管 public(preinstalled/marketplace)、user 管 private + opt-in 链接。admin scope 守 `feedback-admin-scope-user-mgmt`(管共享资源,不碰用户数据)。部门 grant 的授权 UI 归 G。
- **L1 注入(ContextManager 职责)**:`<available_skills>`(name + description),每 lead turn 全量渲染(**无预算闸 —— 见原则 7**;索引大小靠 operator 策展约束,非运行时截断)。接入 `ContextManager.build()`,仿现 `_build_available_subagents`/artifact inventory 模板。**只 lead 需要 skill**(subagent 不注入)。`disable-model-invocation` 的从索引隐藏。
- **L2 = `read_skill(slug)` 工具**(决策 4):独立工具、镜像 read_artifact 契约(`inf` 不二次 persist、`AUTO`),**死简单——返回 `skill_md` 全文、无 path 参数、后端不解 zip**;输出附「其余细节(含 references)须 mount 进沙盒读」提示;**不合并进 read_artifact**(身份空间/store/Manager 不同)。用户 `/skill` = 等价入口(同走 read_skill 取正文注入,多一个 UI 触发)。激活无副作用、非状态切换(原则 8)。
- **`allowed-tools`→成员轴 B 的 enable-only(决策 11)**:skill 激活把 slug 加进新 `state["active_skills"]`(照抄 `always_allowed_tools` 的 path 持久化 —— state→`Message.metadata`→父消息捞回,`controller.py:176/529`;**不扫历史**);resolver 据此**在 agent MD 宇宙内翻开** skill `allowed-tools` 点名的 `disabled` 工具(**不能引入宇宙外工具、不碰等级**)。**resolver 骨架已在 B 立**(agent MD 成员 + tool-set 单元展开,见 Phase B);**C 只给它加一个输入层 `active_skills`**(在 agent MD 宇宙内 enable `disabled` 工具),**不重碰 4 个读点**。dept 减法 / MCP 运行时单元 / 四类 wire-in / UI 同理各阶段加输入层(F/G),非两套重写。**与 `always_allowed_tools`(等级轴的 confirm 抑制)是两条独立轴**,skill 不碰后者(见原则 8/11)。**两个待显式过的语意**(memory 已标):① 历史 tool_result 引用了已不在 `active_skills`/未激活而不可见的工具时的状态文案;② skill `allowed-tools` 点名了 agent MD 宇宙外(未声明/未 `disabled`)的工具 → 忽略 + **import 期校验 skill 工具需求 vs 目标 agent 宇宙并 warn**。
- **config 种子**:`config/skills/<name>/` 启动时 zip→完整 blob + 解析索引列/skill_md→upsert 进 DB(`visibility=public`、`default_enabled=true`、owner=null),内容哈希幂等防重(决策 3)。

**到时再敲定**:frontmatter 精确字段子集(v0 = `name`/`description`/`allowed-tools`?);skill 与 B 的 tool-set 是否联动(**开放分叉**:CC/opencode 完全分离;我方**可选**让 skill 的 `allowed-tools` 调用时 auto-`search_tools` 预载其 set —— v0 倾向保持分离、再议)。

### D — Skill bundle 执行(L3 挂载进沙盒 + `compatibility` 依赖三层 + 离线 wheel)

**做什么**:让带 bundle 的 skill(docx/pptx/xlsx/pdf 类)真能用 —— 模型按需把完整 bundle mount 进沙盒(复用现有 `mount`)、bash `cat` 读 references / `python` 跑 scripts;依赖走原则 4 三层离线投递。这是「沙盒先于 skill」的兑现点。**bundle 里一切(references + scripts)都在沙盒读**(决策 4 判别线:SKILL.md 走读通路、bundle 走沙盒)。

**包含**:
- **bundle 经现有 `mount` 进沙盒(复用,非新机制)**:扩 `mount` 让它能引用一个 skill 的 bundle(类比引用 artifact id),解**完整 zip**(含 SKILL.md/references/scripts/assets)到**固定约定路径** `/workspace/.skills/<slug>/`。`${SKILL_DIR}` 因路径由 slug 确定**在 read_skill 时即可解**(替成约定路径),mount 返回真实路径作运行时确认 —— **占位符问题关掉**(决策 4)。**模型驱动、per-turn**(决策 5/原则 8):`read_skill` 提示"其余去沙盒读",模型那轮自己调 mount;**不 auto-mount、不跨轮维持**,受沙盒 ephemeral 纪律 + `<sandbox_status>` + loud-fail 管。
- **依赖三层兑现**(原则 4 / 决策 6):① 通用重栈进沙盒镜像(pandoc/libreoffice/poppler/qpdf + 科学栈,沙盒 plan B 段镜像扩容);② 离线 wheel bundle 固定位常驻 extras;③ skill asset `wheels/`(长尾兜底):**模型在沙盒里按需 `pip install --no-index --find-links .skills/<slug>/wheels/` 离线装、非系统自动**(守原则 8:激活/mount 无沙盒副作用;装 = 沙盒里一条普通 bash,同 `cat`/`grep` 词表自然消解)。**`compatibility` 字段 = 声明式提示信号、非触发器**:进 SKILL.md/`read_skill` 输出告诉模型「需要什么 + 装不到就用 `wheels/`」,模型据此 probe 环境(`import`/`pip show`)、不满足才装;script `ImportError` loud-fail → 模型补装兜底。前两层(①② 镜像通用栈 + 常驻 extras)已覆盖绝大多数依赖 → 第三层触发频率低,auto 无条件装 = 反向重机制(激活退化成普通 read+mount 的方向回潮)。
- **典型闭环跑通**:用户传 docx → artifact(blob)→ mount → skill python 拆/改 OOXML 或 pandoc 转 → persist 回 artifact。这一条同时练 A(可选,数据工具变体)、C-mount、skill 执行。
- **`substitution` 子集**(可选):若存货/预装需要,实现 `$ARGUMENTS`;`` !`cmd` ``/`${SESSION_ID}` 暂不做。

**到时再敲定**:skill bundle 挂载点(`/workspace/.skills/`)与 artifact mount(`/workspace/<id>`)的命名空间隔离;沙盒镜像扩容清单(node?LaTeX?权衡镜像大小);wheel bundle 的 arch 化(沿沙盒 plan per-arch 纪律);`pip install` 离线在沙盒激活期的耗时/失败处理;CPU 纪律(解压炸弹/大 zip,沿沙盒原则)。

### E — 导入门禁与预装(硬门槛 validator + 软门槛 verify agent + pandoc-first 预装)

**做什么**:skill 的不可信输入门禁(原则 2)+ 把用户存货改造成预装集。**last step**(用户:存货预装前先改)。

**包含**(导入单管线两道门,决策 7):
- **硬门槛 = 确定性 validator**(plain code,**阻塞**,跑每次导入含 user 私有上传):借 `skills-ref`/`skill-validator` 规则 —— 6 字段 schema、name↔dir、孤儿文件、未闭合 fence、内部链接解析、SKILL.md 体量(legibility 警告)、`compatibility`-vs-(镜像栈 ∪ 自带 wheel)交叉校验(气隙网:声明需网即拒/标)。bundle 字节上限归上传路由配额闸(原则 7③,按信任分层、admin 无闸),非 validator 项。
- **软门槛 = verify agent**(**可强制通过**):**职责单一 = 判定「能不能在本系统跑」**,懂本系统约束(沙盒工具词表、无网、mount/persist、tool-set 披露)→ 三查:① 用了没有的工具/harness?② 需要装依赖(`compatibility` 声明 vs 镜像栈 ∪ wheel)?③ asset 放好没?**输出诊断报告(能跑/缺什么),不产 rewrite、不改 body**(改造/简化是人的事,见下条预装集简化 + 原则 3)。**用户私有上传时可选/可跳过**(LLM 成本)。其「系统知识」指向**活文档**(本 plan / skill-authoring 参考),不硬编工具名防漂移。
- **预装集简化(人工,非 verify agent)**(决策 8):预装集就 4 个文档 skill,**开发者在 E 阶段手工分析简化** —— 改 pandoc-first、删三份重复 `office/`、补 `compatibility` 声明、补 asset wheel(若长尾依赖);skill-creator 改造(去 CC widget/subagent-fork 假设)或暂不预装;schedule/setup-cowork **不移植**(驱动 CC 产品 widget)。改完进 `config/skills/`。verify agent 在此只回答「改完这版能不能跑」,**不替人做简化**。

**到时再敲定**:validator 借哪些具体规则、阈值;verify agent 自身是不是个预装 skill(自举);预装集最终名单;ZIP 导入导出端点(决策 6)。

### F — MCP client(把 MCP server 接成又一个 deferred tool-set provider)

**做什么**:落地 MCP 客户端,让系统能对接只暴露 MCP 接口的内网系统。**架构上 B 已把难的那半(registry/披露)铺好** —— F 是 B 的 provider 抽象的第一个外部消费者(自定义 HTTP 工具/tool-set 是内部消费者),MCP 工具 = 又一个 deferred tool-set,走同一 `search_tools`,不重做工具系统。reference 实现 = `../opencode/packages/opencode/src/mcp/index.ts`(本仓库同级,非项目内;provider-agnostic、无 server 依赖,比 CC 更贴我方)。

**包含**:
- **传输 + 协议客户端**(净新,体力活):用 Python 官方 MCP SDK 接 stdio(`StdioClientTransport` 等价)+ http/sse;JSON-RPC 握手、`tools/list`/`tools/call`、`list_changed` 动态刷新、连接生命周期/重连。照 opencode 那套搬。
- **JSON-Schema → 我方 XML 工具描述适配器**(我方特有的一道):MCP 工具的 `inputSchema`(JSON Schema)+ description 渲染成我方 prompt 级 XML 工具描述 —— CC/opencode 把 JSON schema 直接交原生 function-calling,我方是 XML,故须这层。`callTool` 包成我方 `BaseTool.execute` 形。**外部 tool 名 sanitize(reviewer P2)**:`tools/list` 返回的 tool 名是**外部输入**、可能含 `__`/非法字符,撞我方 `<unit>__<tool>` 单分隔规则(server 名 operator 可校验、tool 名控不住)→ 适配器做**确定性可逆 sanitize**(清洗成合法 `<tool>` 段给模型看 + 注册,F 保留原名→`tools/call` 时映射回真名),loud-log 清洗动作。保住工具不丢,优于拒整个 server(太狠)或跳过该 tool(丢能力)。
- **接入 provider 抽象**(B 的回报,近乎免费):打 `source="mcp"` flag、`<server>__<tool>` 命名(`__` 唯一分隔、不带 `mcp__` 前缀,决策 11;server 名跨 provider 全局唯一、撞名 loud-fail)进统一 registry;整 server = 一个 deferred tool-set,索引行常驻、`search_tools` 按 server 名搜加载。**MCP 工具太多的披露问题在 B 落地即解决,F 不再碰**。
- **server 配置**:MCP server 由 operator 显式声明(同 `config/tools/` 自定义工具的 operator-config 哲学),per-deployment;无联网发现(Non-goal)。
- **部门作用域 wire-in(G 的消费者)**:MCP server 接入 `department_resource_rule`(`resource_type=mcp`),方向 = 规则行显式 `effect`(server `visibility` 只定未列出部门的默认,决策 10;**F 给 server 配置加 `visibility` 字段**)。整 server 单元粒度(声明/减/加/enable 都在 server 粒度)= 决策 11 解 MCP 动态工具的同一抓手。消费(查表 + 树解析)在 G。
- **动态工具的权限粒度**(决策 11 已定调):MCP 工具动态、没法逐个静态枚举 → 权限锚在 **server 单元**(agent MD 静态声明 server 单元、m2m 减整个 server、运行时发现的工具填进**已授权单元**),即 `<server>__*` 整组粒度、**不逐工具**。比 memory `permission-param-granularity-direction`(工具名+参数模式、仿 CC allow-rules)粗一层:server 粒度够 v0,更细留将来。

**到时再敲定**:MCP server 配置落点(新 `config/mcp/` 还是 `.env`/统一 config);stdio vs http 在内网的取舍(DooD/容器内进程 vs 网络服务);权限 set 粒度的具体语法(与 B 的 `always_allow` 跨 set 语意对齐);`list_changed` 刷新后历史里旧 tool 描述如何过期/自纠(**无「已发现集」状态机**,决策 2;靠常驻索引行变化 + 模型重 `search_tools` 自纠);凭证注入(同 HttpTool 的 `{{TOOL_SECRET_*}}` env 模板,operator 级密钥;**用户级身份透传是另一根轴、推迟**,见 Non-goals)。

### G — 部门作用域授权 + 管理 UI(横切:skill/tool/tool-set/mcp 四类一套机制)

**做什么**:把"哪个部门能用哪些资源"做成**一套统一的部门作用域授权层**(决策 10),服务 skill/tool/tool-set/mcp 四类;agent MD 不动,引擎运行时组合有效集。这是用户要的"admin 给指定部门配特定工具"——**靠作用域涌现,不克隆 agent**。

**包含**:
- **统一 `department_resource_rule` 表 + 解析**(地基随 C 落):`(department_id, resource_type, resource_id, effect)` polymorphic(决策 10 已论证为何一张表对而非将就);判定 = 用户 `department_id` 走 `parent_id` 祖先链 ∩ 规则集(指父覆盖子树;命中后方向 = 行显式 `effect`,未命中走单元 `visibility` 默认),解析与已加载资源取交集(陈旧规则自然成空)。
- **`EffectiveToolset` resolver:G 只加 dept 规则输入(骨架在 B、决策 11)**:resolver 骨架(唯一解析点、读点 = `ctx:89`/`ctx:204/215`/`engine:759`/`engine:844` **+ `search_tools` 可见集过滤**[B,reviewer P1]读同一输出)**已在 B 立**、C 加了 `active_skills` 输入、F 加 MCP 运行时单元 —— **G 在此只追加最后一个输入层 `dept 规则`**(按目标单元 `visibility` 做 grant/deny 例外,决策 10:`(agent_config, active_skills, registry+MCP 运行时, dept 规则)` → 展开单元 → 扁平 `{tool: level}`),**不重碰读点**(由构造消灭多消费点漂移正是把收口提前到 B 的收益)。dept 规则的特殊处 = 它是 resolver 唯一**双向**(grant/deny)输入(C/F 都是 enable/填充),须在单元展开后按 `<unit>__*` 前缀整组应用、方向 = 行显式 `effect`(visibility 仅定未列出部门默认)。
- **四类接入**:skill(C,`visibility=department` 消费 grant)、tool-set(B wire-in)、tool(粒度需要时)、mcp(F wire-in)。
- **管理 UI**:admin 按部门分配资源(grant 增删)+ 看每部门有效能力;前端复用 skill 管理页骨架(决策 1 的设置/管理页),按 `resource_type` 分 tab。admin scope 守 `feedback-admin-scope-user-mgmt`(管共享资源/授权,不碰用户数据)。
- **规则体检 check 端点(admin 只读审计,用户提)**:扫 `department_resource_rule` 报两类卫生问题——① **孤儿**:`resource_id` 指向的 skill/tool-set/mcp 已不存在(删资源留的悬空行);② **冗余/失效**:资源还在,但行 `effect` == 该资源 `visibility` 的默认方向(`public`+`deny`=有意义、`public`+`grant`=冗余;`department` 反之)→ 多半是 visibility 漂移后留下的 dead row。**`effect` 显式列正是这个端点的前提**(方向派生时永远"自洽"、漂移被掩盖,存了显式 effect 才比得出来)。**只读报告、非门禁**(正确性已由 effect 列按构造保住,端点只供 operator 决定清理);清理是独立 admin 动作、**config 类绝不自动删**(决策 10 运维契约)。**HA 真相分层**:skill 类查 DB=权威;config 类(tool-set/mcp)查各容器 registry=**best-effort**(滚动更新窗口可能误报瞬态孤儿,标 advisory,同决策 10 per-container 真相)。

**到时再敲定**:grant 校验各 registry 的接口(config 无表如何查 tool/toolset 存在);**grant 删除同步落点**(决策 10:skill 删除 Manager 同事务 cascade `department_resource_rule` = 绑业务事件 HA 安全;config 类**系统完全不自动碰 m2m**[内存 registry 无单一真相、归 operator 运维契约:改 config 自己同步 grant]、靠解析交集保正确;skill 类(高频增删)= 应用侧 cascade + DB 真相 UI 孤儿提示可靠;ABA-id 根治是否做)的实现归属(skill cascade + UI 孤儿提示随 C/G,config 类系统侧零机制);引擎"组合有效集"的缓存粒度(部门/用户级,资源变更少、可缓存);UI 是否支持"部门继承"可视化(指父覆盖子的展示);单工具粒度(`resource_type=tool`)v0 要不要,还是只到 tool-set 粒度;**树规则 override 优先级**(深层更具体部门能否翻案祖先规则)—— v0 不做、祖先一刀覆盖整子树(决策 10"指父覆盖子树"),将来要"二级禁/某三级特批"再引入"最具体规则胜"。

## 关键风险

- **披露与 skill 职责漂移**(B/C)—— 一旦把披露塞进 skill,就回到「动态工具对小模型 legibility 负资产」的老坑。守原则 1:披露是工具层、skill 是覆盖层,review 时盯死边界。
- **依赖气隙地雷**(D)—— 社区 skill 默认运行时联网 `pip install`,搬进气隙网必断。`compatibility` 声明 + validator 交叉校验是唯一防线;存货零 manifest = 预装集须人工补全依赖声明。
- **bundle = 新不可信输入类**(E)—— skill 脚本在沙盒跑(隔离 OK),但「哪些 skill 准入」是 trust 决策,生态无 signing 层 → 门禁(确定性 validator + verify agent 可运行性评估 + 人审/人手改)是我方唯一 trust 边界,不可省。
- **body 静默改写诱惑**(C/E)—— 工具词表对不齐时,regex rewrite body 看似省事实则脆(改坏比标记坏)。守原则 3:lint 标记 + 人审,绝不运行时静默改。
- **`search_tools` 无 server `tool_reference`**(B)—— 我方纯 prompt 级模拟。**不维护「已发现集」状态机**(决策 2 / changelog 06-17;修正旧表述"追踪须自建"误导实现者造状态机):描述随 tool_result 留历史、被压缩则模型见索引行自己再 search(自纠);避开 server beta 依赖、也不引入状态管理。
- **沙盒镜像膨胀**(D)—— pandoc/libreoffice/科学栈 + 可能 node 烤进镜像,镜像大小与构建时间。权衡「通用即烤、长尾走 wheel」,别把长尾也烤进去。
- **MCP server 粒度授权偏粗**(F/G)—— 决策 11 已定调把 MCP 当 **server 单元**(声明/减/enable 整组、resolver 展开),解了"动态工具无法静态枚举"。**残留风险 = 粒度只到整 server**:授/减一个 server = 它所有工具一起动,**没法逐工具选**(逐工具静态列举对动态发现不可能);更细的工具名+参数模式授权(`permission-param-granularity-direction`)留将来。**与 skill/部门同根**:三方都要引擎从"静态 MD dict 查"改"resolver 解析"——统一解 = 决策 10/11 的 `EffectiveToolset` resolver(**骨架 B 立、C/F/G 各加一个输入层、读点只收口一次**,见 Phase B),别三处各修读点。
- **MCP server = backend 侧网络/凭证边界**(F)—— MCP client 跑在受信 backend(非沙盒),http MCP server 是真实出网 + 凭证持有点,与沙盒「全禁网」正交。须按现有 web 工具的出网纪律(凭证 env 模板、operator 显式配、内网链路审计)管,别让 MCP 成为绕过沙盒网络边界的后门。

## 变更日志

- 2026-06-16 起草。前置调研三件(参考实现 CC 工具披露+MCP、开放标准 agentskills.io 现状、用户存货)做完后定调。锁定 9 决策 + 7 原则,核心 = **原则 1「披露归工具层、skill 正交」**(CC 实证:`SkillTool` 零引用 deferral;披露 = deferred-tools+`ToolSearch`)。三 scope(private/preinstalled/marketplace)= DB + config 种子,market 降维成离线 link 动作。依赖继承沙盒原则 7 三层离线投递 + `compatibility` 声明。导入门禁二分(确定性 validator 每次 + 监督式 adapter skill 策展)。采标准 6 字段、v0 最小(name+description)。5 阶段:A 工具结果→artifact(独立前置)/ B 工具披露(tool-set+search_tools,留 MCP provider 缝)/ C skill 核心(存储+L1/L2+权限覆盖)/ D bundle 执行(L3 挂沙盒+依赖三层)/ E 门禁+预装(pandoc-first)。粒度 = plan 级。**开放分叉**:skill 是否联动预载 tool-set(v0 倾向分离)。
- 2026-06-16 **分支策略定:走 main**(用户拍)。本 plan 无破坏性中间态(不同于沙盒 plan 的 md→Word 半迁移态),A/B/C 纯加法,逐阶段直接合 main、再 overlay intranet,不开长命特性分支。
- 2026-06-16 **激活机制收敛(opencode 调研 + 三轮讨论)**:补调研 opencode(provider-agnostic,与我方同不靠 server 端特性),印证两点 ——(a) 激活 = 返回正文的 tool_result(opencode `skill` 工具返 `<skill_content>`、纯文本可移植),非状态切换、唯一副作用 permission 检查;(b) opencode **零工具披露**(全量发所有 builtin+MCP schema)→ 证明 provider-agnostic 世界无 30-endpoint 现成解,我方 B 是净新;且我方 XML prompt 格式使 prompt 级 deferral 比 CC/opencode 都自然(本就自己渲染、deferred 即不渲染)。据此推翻原决策 4/5:**① L2 = 独立 `read_skill(slug)` 工具**(镜像 read_artifact 契约、`inf` 不二次 persist、`AUTO`;**不合并进 read_artifact** —— 身份空间[user-scoped slug vs session-scoped id]/store/Manager 不同,合并要么加 type 参数违 legibility 要么藏第二 Manager;输出 = 正文 + bundle 文件清单 + mount 提示 = L2→L3 桥)。**② L1「有哪些 skill」归 ContextManager**(仿 artifact inventory 先例,**不学 opencode/CC 塞工具描述**)。**③ bundle 走现有 `mount` 复用、模型驱动 per-turn**,删 auto-mount、删跨轮 mount 一致性(沙盒 plan 已为此打过仗收手,不重建)。新增**原则 8**:激活轻量、无沙盒副作用、不豁免 per-turn ephemeral。净结果——「激活」退化成「一次普通 read + 一次普通 mount」,零新机制。阶段 C/D 同步改。
- 2026-06-16 **加 F 阶段:MCP client(不 defer,正式入主线,用户拍)**。调研确认 opencode 做了全套 provider-agnostic MCP client(stdio/http+OAuth、`listTools`、`mcp__` 命名、`dynamicTool` 转换、`list_changed`)但**零披露**(全量发)—— 证明 provider-agnostic 世界无 30-endpoint 披露现成解,我方 B 即补此。难度评估:**架构层近乎免费**(B 的 provider 抽象 + `search_tools` 就是为「MCP = 又一个 deferred tool-set provider」留的,F 是其第一个外部消费者、纯加法),**剩体力活** = ① MCP SDK 传输/协议客户端(照 opencode 搬)② JSON-Schema→我方 XML 工具描述适配器(我方特有,因走 prompt 级 XML 非原生 FC)③ server operator 配置 ④ **动态工具权限粒度**(静态白名单容不下动态 MCP 工具 → set/server 粒度授权,推上 `permission-param-granularity-direction`)。F 依赖 B、与 skill 线(C/D/E)正交可并行。Non-goal 收窄为「联网 MCP registry/公网自动发现」(client 本身入范围,server operator 显式配)。reference = opencode `mcp/index.ts`。
- 2026-06-17 **存储/读取/导入三处收敛(用户拍)**。**① 存储(决策 3 重写)**:索引列(name/description/allowed_tools/slug/scope/owner)+ `skill_md` TEXT 全文列(L2 读免解 blob)+ **`bundle` BLOB = 完整原始 zip(含 SKILL.md)**;**无损导出是 construction 保证的**(直接吐原 blob、不从列重序列化 frontmatter,未知字段不丢),代价 SKILL.md 三处冗余(小文本,值)—— 推翻上轮"zip 剥掉 SKILL.md"。**② 读取(决策 4 收紧)**:判别线 = **SKILL.md 走读通路、bundle 里一切(references+scripts)走沙盒**;`read_skill` 死简单(返回 skill_md 全文、**无 path 参数、后端不解 zip**),references **不再走读通路**(推翻上轮),一律 mount 进沙盒 `cat`/`python`;故 **C/D 阶段线 = 有没有 bundle**(单 SKILL.md→C 不碰沙盒;有 bundle→D),比上轮"references 在 C/scripts 在 D 劈一个 skill"干净;存货实证零损失(带 references 的 skill 全也带 scripts)。**③ 占位符**:mount 用**固定约定路径** `/workspace/.skills/<slug>/`,`${SKILL_DIR}` 因路径由 slug 确定在 read_skill 时即可解、mount 返回真实路径作确认 —— 占位符问题关闭。**④ 导入(决策 7 重写)**:单管线 `upload→unzip→硬门槛(validator 阻塞)→软门槛(verify agent 可强制通过)→入库`;软门槛对用户私有上传可跳过(LLM 成本)、admin 预装时是策展步。阶段 C/D/E 同步改。
- 2026-06-17 **部门作用域授权入主线(新阶段 G + 决策 10 + 决策 1 重写,用户拍)**。起因:用户问 agent/工具权限能否 config-默认 + UI-改、按部门定制。**定调**:① **不克隆 agent**——agent 仍 config(`agents are data`不破),部门"定制 agent"靠**作用域涌现**(部门 grant 的 tool/toolset + 部门可见 skill)。② **一套统一部门授权机制服务 skill/tool/tool-set/mcp 四类**,而非各做一遍。**数据模型(决策 10)**:统一一张 polymorphic `department_resource_rule(department_id, resource_type, resource_id)`——**论证为何一张表对而非将就**:四类资源异构(只 skill 是 DB 行,tool/tool-set/mcp 是 config 按 name、根本无表可 FK),polymorphic 丢 FK 的缺点被消解(3/4 无 FK 目标;skill 陈旧 grant 解析成空无害);判定 = 用户 `department_id` 祖先链 ∩ grant(指父覆盖子树);**agent MD 不动**,引擎组合有效集 = MD 基础 ∪ 部门 grant ∪ skill allowed-tools,权限走现有 `tools.get(name, tool.permission)` 回退。**决策 1 重写**:三 scope 拆成 `visibility`(private/public/department)+ `default_enabled` 两正交字段(preinstalled=public+默认开、marketplace=public+默认关)+ 稀疏 `user_skill` 覆盖(link 与 toggle 同一机制)。**横切**:解了"静态 agent 白名单被 skill/MCP/部门作用域三方压垮"——决策 10/G 是统一解,别三处各修(关键风险 MCP 条已并)。**阶段 G** = department_resource_rule 表+祖先链解析(地基随 C 落)+ 引擎有效集组合 + 四类 wire-in(skill@C / toolset@B / mcp@F)+ 管理 UI;依赖 C(地基)+ B/F(资源存在)。**推迟(Non-goal)**:用户身份/凭证透传(B1 用户字段 + B2 OAuth 金库,与"能否用工具"正交的身份轴,红线=只 backend 工具、沙盒永不拿凭证)、运行时可编辑 agent 定义(仅部门需不同 prompt/model 才需,单独 plan)。阶段 B/C/F 同步 wire-in 注记;目标范围 + 进度表 + 关键风险同步。
- 2026-06-17 **skill 能力持有机制定:复用 `always_allowed_tools` 的 path 持久化,不扫历史(用户拍)+ 原则 7 收缩**。起因:讨论激活的 permission 副作用怎么跨 turn,先纠一个事实——**「始终允许」本就跨 turn**(`always_allowed_tools` 回合末写 `Message.metadata`、下回合父消息捞回,`controller.py:176/529`,无 schema 改动),"无跨 turn 用户 prefer 状态机"前提不成立。**拆两条权限轴**(`engine.py:843`):轴 1 能力白名单(skill `allowed-tools`/dept grant 并入)/ 轴 2 confirm 抑制(`always_allowed_tools`,skill 碰不到 —— 授 Bash≠自动批准)。**能力持有 = 新 `state["active_skills"]` slug list,照抄 `always_allowed_tools` 生命周期**(否决了上一秒提的"每回合扫 `EventHistory` 现推"——那是 LLM 上下文重建路径、非权限热路径,O(1) state 判断才对)。副带修暗坑:派生版让压缩**静默撤权**,独立 `active_skills` 持有则 body 随压缩走、**能力轴不被压缩误伤**,两件事解耦。护栏:`active_skills` 只管能力轴、**不复活 L3 mount**(带能力跨 turn ≠ 带 mount 跨 turn)。v0 append-only sticky、显式停用 defer。原则 8 重写 + 决策 10/Phase C line 115 同步(后者原把 `allowed-tools` 误映射成 `always_allowed_tools`、两轴混淆,已纠)。**另:原则 7 收缩**——私有化部署无本地 tokenizer(全系统 token 判断靠推理框架返回的 usage、事后量),**删调用前 token/L1 索引预算**(token 溢出由现有 compaction 反应式吸收;L1 靠 operator 策展非运行时截断,按预算丢=silent eviction 自相矛盾);**唯一硬上限 = bundle 字节、按信任分层**:用户私有上传走现有 per-user blob 配额 + 413,**admin 策展 skill 无配额闸**(preinstalled 走 config 种子不经上传、marketplace admin 上传对 per-user 配额豁免,合法携带离线 wheel);bundle 字节闸归上传路由、非确定性 validator 项(否则拦了 admin 大依赖包)。**追加(同日,核实代码后)**:轴 1 实为**一个 dict `{name: level}`**——key-presence 同时管渲染(`context_manager.py:89`)、执行闸(`engine.py:759`)、等级(`844`),"禁用"=不放 key、无独立 disable 标志也不需要;故组合"加性并入"且**只能加、走自带默认级,永不能降 CONFIRM→AUTO**(降级唯一路径=用户始终允许)。**实现坑**:有效 dict 须同喂两读点(context_manager 渲染 + engine 闸/级),漏一即漂移 → 修正原"一处改动"为**一处组合两消费点**。决策 10/Phase G/原则 8 同步。
- 2026-06-17 **工具权限模型定稿:两正交轴 + 四层链 + `EffectiveToolset` resolver(新决策 11,跨数轮收敛、用户拍「自洽」)**。多轮逼出最简自洽形:**轴 A 等级 `{auto,confirm}` 唯一来源工具定义、config 全程不可改,唯一运行时变更 = 用户始终允许 `confirm→auto`**;**轴 B 成员 `{enabled,disabled}` = agent MD 写 / m2m 减 / skill enable-only**。**四层链逐层只收窄**:工具定义(能跑什么+等级)→ agent MD(最大宇宙=天花板)→ m2m 减法(default-allow 收回式)→ skill enable(翻开 `disabled`、不加 key 不碰级)→ 运行时始终允许。**关键收敛**:① 用户一刀「agent MD 也不覆盖等级、只 enable/disable」——核实四个 agent MD 的级**全等于工具默认**(覆盖能力本就空转)→ **零损失**,且把我之前误塞进 level 枚举的 `disabled` 正确拆成**独立成员轴**(`ToolPermission` 保持两值)。② m2m = **减法/收回**(非决策 10 原「发放」)→ **决策 10 语义翻**:本表对 `tool/toolset/mcp` 行 = deny(default-allow),对 `skill` 行 = grant/可见性(default-deny,决策 1),polymorphic 表按 `resource_type` 分派语义(拆不拆表=到时再敲定)。③ MCP「撑裂纯减法」的担心**被用户证伪**:MCP/tool-set 是 dict 里的**单元**,agent MD 声明单元、m2m 减整个单元、运行时发现只是往**已授权单元**填内容,无加法通道;等级 per-tool 来自定义(MCP=server 级默认)。④ **存储=单元引用、resolver 展开成扁平 `{tool:level}`**,所有读点(渲染/条件段/闸/级)读同一输出,`engine:844` 塌成 `tool.permission`。陷阱(confirm 洗 auto)**按构造消失**(全链无人能改等级)。落点:决策 11(新)+ 决策 10 语义翻 + 原则 8 轴重写 + Phase C/G 同步。**迁移≈零**(现有 `name:auto/confirm` 按 enabled 读)。
- 2026-06-17 **`search_tools` 跨 turn 靠历史、非追踪状态(用户提)**。问:发现的工具也要跨 turn,但不该套 `always_allowed_tools` 那套。定:**`search_tools` 返回值复用 `generate_tool_instruction`(=系统提示词渲染工具那套),完整描述作 tool_result 留在历史**,后续轮模型自然可见 —— **不维护「已发现集」、不重渲染进 system prompt**,被压缩则见常驻索引行自己再 search(自纠,同 mount 不跨轮哲学)。**与 `active_skills` 的对照(看似同类、结论相反)**:发现是**上下文**问题(描述在不在 context)→ 家在历史;`active_skills` 是**权限**问题(决策 11 轴 B,hot-path + 不被压缩误撤)→ 要 metadata-state。**关键解耦**:工具可调与否由 resolver 按 unit 成员判、与发现无关(deferred 只是描述没渲染、闸照过)。删 Phase B「已发现工具名追踪载体 + compaction 存活」待定 + 决策 2 的 `extractDiscoveredToolNames` 追踪;附带 system prompt 恒索引行 = 缓存前缀稳。落点:决策 2 + Phase B `search_tools` bullet/到时再敲定。
- 2026-06-17 **tool-set 成员命名空间 `<setname>__<tool>`(用户提)**。问:tool-set 工具要不要特殊 call name(如 `set:tool`)。定:**要,且镜像 MCP `mcp__server__tool` 用双下划线 `<setname>__<tool>`、loader 自动加前缀(作者写裸名)**。**核心理由不是防撞名,是让 unit 粒度授权按前缀落地**:决策 11/Phase F 的「单元粒度减法/enable」靠 `<setname>__*` / `mcp__server__*` 前缀整组匹配,不带前缀 resolver 没法把一个 set 拎出来;次要=跨 set 同名不撞 + 模型可读。**用 `__` 不用 `:`**:核实 `xml_parser.py` —— 工具名是 `<name>` 文本内容(冒号 happy path 尚可)但参数是 XML 标签、且 `_repair_tool_name_as_tag` 修复路径把名当标签,冒号撞 XML 命名空间崩;`__` 标识符处处安全、单 `_` 在名内双 `__` 作分隔故扁平 builtin 仍可区分。落点:Phase B tool-set 格式 + 决策 11 单元段交叉引用。
- 2026-06-18 **披露两层定调 + tool-set ↔ MCP 同型显式化(用户问 MCP 形态/是否要三层 discovery)**。澄清 **MCP `tools/list` 协议层扁平一发返全部**(name+desc+JSON-Schema)、无原生渐进发现 → 我方管理的是 **context token 非网络往返**,故无需三层。**定:披露两层**——① 检索注册表(`search_tools`)全成员扁平注册(MCP 连接灌/tool-set 启动灌)、② L1 索引按 unit 选展开/折叠(`default_enabled` 延伸、大 unit 折一行防撑爆);**unit 是授权+生命周期边界、非 discovery 边界**,「server 里有啥」= `search_tools(unit=x)` 普通查询,**不做 `list_members` 第三层**(完整 usage 已由 search_tools 返回 = 那层)。**tool-set ↔ MCP 同型**显式写入(授权/披露/命名整链对齐,唯一别 = 静态[config 已知] vs 动态[连接灌注册表])——此前散落于 line 19/103/152/决策 11 暗示、无中心声明,这轮补齐。落点:Phase B 新 bullet「单元非 discovery 边界」+ 决策 11 单元段同型概括。**命名两段** `<unit>__<tool>` 够用(unit 名系统内已唯一,前缀 `<unit>__*` 即整组授权),不做三段 `mcp__`/`set__` 来源标识(对模型/授权零增益)。
- 2026-06-18 **依赖第三层 = 模型驱动按需装、非激活自动(用户问 auto 还是 compatibility 提示模型检查)**。定:**不做系统 auto `pip install`**——`wheels/` 离线装是**模型在沙盒里按需跑的一条普通 bash**,非激活/mount 触发。理由:① 原则 8 激活/mount 无沙盒副作用(auto 是副作用,且 read_skill 那点无沙盒上下文、mount 是通用机制不塞 skill 专属 pip);② 沙盒工具词表自然消解(pip = cat/grep 同类、非系统机制);③ ephemeral per-turn,auto = 每轮无条件付,probe 才装只在真需要付;④ `ImportError` loud-fail 兜底比盲装稳;⑤ 方向一致(激活退化成 read+mount 零机制,auto 是重机制回潮)。**`compatibility` = 声明式提示信号、非触发器**(进 SKILL.md/read_skill 输出,模型据此 probe 环境、不满足才装);前两层镜像栈+常驻 extras 已覆盖绝大多数 → 第三层触发频率低。修 Phase D line 128「③…**激活时** pip install」措辞(暗示 auto、与原则 8 张力)→ 改「模型在沙盒按需装」。
- 2026-06-18 **verify agent 职责收窄 = 纯「能不能跑」评估,改造/简化归人(用户纠正)**。纠正我把 verify agent 拔高成「会 rewrite 的 adapter」之误:**verify agent 只做一件事 = 判定 skill 能不能在本系统跑**,三查 —— ① 用了没有的工具/harness?② 需装依赖(`compatibility` 声明 vs 镜像栈 ∪ wheel)?③ asset 放好没?**输出诊断、不改 body、不产 rewrite**。**预装那几个 skill 的分析简化(pandoc-first、删三份 `office/`)= 「我们」E 阶段人工的工程动作**(就 4 个、值得手工调),不交给 verify agent。残留硬耦合 = 确定性 lint 标记 + **人手改**(原则 3「绝不静默 rewrite」)。trust 边界三件 = 确定性 validator + verify agent 可运行性评估 + 人审/人手改。落点:决策 7/8 + Phase E 软门槛/预装集两 bullet + 原则 3 + 风险「bundle 不可信」条措辞对齐(删全部「adapter 会改造」暗示)。
- 2026-06-18 **6 标准字段全部 DB 落位、按消费分流;`license` 归 `metadata` JSON 杂项列(用户拍)**。澄清"v0 最小"≠ 只解析两字段:**存储层全 6 字段(乃至未知扩展)无损保留**(blob 真相源,construction 保证),**DB 反规范化列按"系统消费与否"分流** —— 消费的开独立列(`name`/`description`/`allowed_tools` + `compatibility` 供气隙校验),**系统用不上的(`license`/`metadata`/`version`/未知扩展)全归一个 `metadata` JSON 杂项列**("用不上的扔 metadata")。**关键边界**:这是 DB 反规范化层归类,**不改 SKILL.md 原 frontmatter 结构**(`license` 在标准里是顶层、就留顶层,DB metadata 列只是副本,挪字段=silent rewrite 违原则 3);导出仍从原 blob 无损吐。**硬依赖只 name+description**(社区/存货现状其余常空),消费宽容缺失。落点:决策 3(四处存储:消费列 + metadata JSON + skill_md + blob)+ 决策 9(v0 最小→全落位按消费分流)+ Phase C line 114 列定义同步。
- 2026-06-18 **`department_resource_rule` 软引用同步:无 DB CASCADE → 应用层 cascade + reconcile + ABA 防护(用户问删 skill 能否 cascade 删 m2m)**。确认 polymorphic 表 resource_id = name/slug 软引用(tool/toolset/mcp 无表可 FK,skill 也统一不建 FK)→ **删资源不自动删 grant 行**。原决策 10「无害可清」只对一半:解析 `∩ 已加载` 让悬空 grant 功能空转(对),**但漏了 ABA 复活隐患** —— 删 `foo`→悬空 grant→新建同 slug `foo`→旧授权嫁接新实体(default-deny skill = 越权可见)。**补对策分层**:skill(有删除时机)= 删除 Manager 同事务 cascade 清 grant(ABA 窗口=0、范围只 department/public skill);tool/toolset/mcp(config 无删除事件)= 解析取交集 + 启动/定期 reconcile 清孤儿。ABA-id 根治(skill grant 存不可复用 DB id)标开放。落点:决策 10「FK 消解」段展开 + Phase G「到时再敲定」加删除同步实现归属(skill cascade@C / reconcile+UI@G)。
- 2026-06-18 **修正:config 类 grant 不自发 reconcile-delete(HA 滚动更新陷阱,用户提)**。上一条提的「启动/定期 reconcile 清孤儿」在 HA 下有 bug:删 config tool `bar` 滚动更新时,先起的容器 A 用新 config 判 `bar` 孤儿 → 删共享 grant,但容器 B 还跑旧 config 仍用 `bar` → **A 基于单副本局部视图删了 B 还需要的共享数据**(且 grant=operator 意图、删=丢)。**判别线 = 同步绑业务事件还是容器生命周期**:skill cascade 绑删 skill 的 DB 事务 → 提交即全局一致、**HA 安全**;reconcile-delete 绑容器启动 → 滚动窗口内各副本 registry 视图不一致 → 破坏性删除基于局部视图 = 错(「启动 vs 定期」无关,定期 cron 跑在旧版 pod 一样误判;真正要求 = config 全局收敛后单一权威触发)。**改**:config 类 ① 功能正确性靠解析 `∩ 已加载`(各容器各自对、悬空空转无害、不依赖删除、HA 安全)② 垃圾/ABA = UI 标记孤儿 + operator 手动清 / 部署收敛后单一权威 reconcile,**绝不每 pod 自发删**(合 loud-failure:自动删 grant = 静默丢配置)。落点:决策 10 对策段重写 + Phase G「到时再敲定」同步。
- 2026-06-18 **孤儿标记读时实时算、不持久化 orphan 字段(用户问要不要 DB 字段 flag)**。澄清:加 orphan 字段、容器启动检查不一致就 flag = 上一条同一 HA 陷阱的「写 flag」变体 —— A 起新 config 标 `bar` 孤儿、B 旧 config 仍在用 → flag 固化了 A 的局部判断、对 B 是错的;回滚后还残留。**正确 = 孤儿性是派生状态**(`grant ∖ registry` 差集,与解析 `∩ 已加载` 同一运算),**读时(UI 渲染/审计)实时算、不写 DB**:每副本用自己 registry 算、如实反映 config 是否收敛,收敛后自然一致,零字段零残留。operator 看实时标灰行手动 `DELETE`(共享 DB、全局生效)。通用原则:派生状态(grant 对可变 registry 的函数)在 HA 下必须读时算,持久化 = 缓存失效 + 分布式不一致双坑。落点:决策 10 对策②段。
- 2026-06-18 **修正再修正:实时算也救不了 config 类(admin 请求打到哪个容器抖动)→ 按真相源分 + step-back 退 scope(用户戳破)**。用户指出实时算没解决:admin 管理请求被 LB 打到任意容器,A[新 config]说孤儿、B[旧 config]说不是,孤儿列表抖动 —— 我前面把 skill/config 一锅端错了。**正解 = 按孤儿判断的真相源分**:**skill 源=DB(共享单一真相)→ 实时算 + cascade + UI 全 HA 可靠**;**config 类(tool/toolset/mcp)源=各容器内存 registry(滚动窗口多版本并存)→ 物理上无单一真相,实时/持久化/任意容器都救不了**(这是滚动更新事实、非机制问题)。config 类:功能正确性靠解析 `∩ 已加载`(各容器对自己对、不需单一真相、HA 安全),清理只在 config 收敛稳态有确定答案 → 绑部署收敛后单一权威 job、不放 admin 实时 UI。**关键 step-back(design-creep)**:这已是第三轮给 config 孤儿管理打补丁(reconcile-delete→实时算 flag→本轮),悬空 grant 本就无害(解析空转),为「清垃圾/防 config-ABA」小价值造 HA 一致孤儿管理 = fix 复杂度远超 feature 价值 → **v0 退 scope:config 类 best-effort 接受悬空长存、不做孤儿管理机器,admin UI 孤儿提示只对 skill 类**。落点:决策 10 对策②段重写 + Phase G 同步。
- 2026-06-18 **退到底(用户拍):系统只 cascade skill 高频侧,config 类系统完全不碰 m2m = operator 运维契约**。在「按真相源分」基础上用户进一步收 scope:不要部署收敛 job、不要 best-effort 自动清,**config 类系统侧零机制** —— operator 改 config(增删 tool/toolset/mcp)时**自己记得同步 m2m**(运维契约,自洽于「config 即真相、operator 显式配」:改 config 与改 grant 同一人同类动作)。系统只对 **skill(DB 实体、高频增删)做应用侧 cascade**(同事务删 grant、DB 单一真相 HA 安全)。功能正确性始终靠解析 `∩ 已加载` 兜底(悬空空转无害)。admin m2m UI 对 config 类 = 普通 grant 增删(operator 凭认知管),孤儿提示只对 skill 类。落点:决策 10 对策②段 + Phase G 收尾(config 类系统侧零机制)。
- 2026-06-18 **Phase A 首个数据工具改 web_fetch 文件旁路(替假想 HTTP 工具,用户拍)**。原 line 91 的「config/tools HTTP 自定义 API 工具 + operator MD 声明 persist」是个还不存在的假想消费者 → 换成**现有 web_fetch 的文件旁路**做首个验证消费者:web_fetch 走 jina 提网页正文、对 PDF/二进制 URL 本就坏,加旁路按响应 `Content-Type` 自决(非 text/html → 直连下载置 `ToolResult.artifact`、绕 jina)。优点:零新工具 + 补真实缺口 + 端到端串 A→mount→D(pdf skill 输入)+ 一把测 blob/配额/content_type 派生。**persist 决策 = 内建工具运行时自决**(非 operator MD 静态声明、非模型参数)= line 88 声明式的另一合法形态。注:只有网部署有效(内网 web 工具禁用,intranet 入境靠沙盒 persist),HTTP 自定义工具降为未来真有 API 工具时的后续接入。落点:Phase A line 85 + line 91 重写。
- 2026-06-18 **web_fetch 旁路:路由用尾缀非 Content-Type(核实代码后修正方向)**。用户问按 Content-Type 还是尾缀、记得「解 content type 卡很久」。读 `web_fetch.py` 坐实:现状**就是用尾缀**(`_detect_content_type:168` 看 `.pdf`)、全文件零 Content-Type 解析 —— 印证那个坑当初已被避开。定:**路由判断(走 jina 还是文件旁路)用尾缀**(零成本、不发探测请求、绕开两类卡顿:为拿类型额外 HEAD / 读 body 触发编码探测);**artifact content_type 顺手读下载响应头、尾缀兜底**(区分:提前探测=可能卡 vs 已在下载顺手读 header=免费)。旁路插 Jina 之前分流(文件不进 jina),复用 `_fetch_pdf:352` 直连下载 + `_read_capped` 封顶,抽文本换存 blob、文件按 bytes 绝不 `.text`。行为变更:PDF 从抽文本→blob 句柄(对齐上传 blob-only `:384`),可选附文本预览。修 plan line 91 原「Content-Type 优先、扩展名兜底」(方向写反)。
- 2026-06-18 **工具披露走 CC 激进路线 + tool list 挪动态 reminder(prompt-caching,用户拍)**。讨论两点定案:**① 索引行 = tool-set 一条 set 级 description + 成员纯工具名(光名字、不给每工具 desc)**,单工具 deferred = 光名字 —— 对齐 CC(赌名字自解释),我方 tool-set 名外包 set desc 做语境比 CC 孤立名字稳;完整描述 search_tools 按需补(推翻上一轮「名+一句描述」)。**② tool list 从 system prompt 前缀(`context_manager.py:92` system_parts)挪进 `_build_dynamic_context`、与 `artifacts_inventory`(`:209`)同级**(历史末尾 system-reminder)。动机=skill enable/dept 减法让工具列表会话内动态,留前缀每变一次打掉整段历史 prompt cache;挪后只失效末尾。**核实坐实**:artifact inventory 早在 reminder、`:77` 注释明立「动态放 reminder 别坐前缀」,工具描述是唯一没跟上的;整块挪免去 baseline/动态分层(否决我打断前那版绕的分层黄金律)。**reminder 纪律**:task_plan 打头锚 task、tool list 进资源区保持索引体量,免淹没当前 task(用户担心点)。**现实**:生产 6/11 关 APC → 收益暂 0 但无害,未来重开自动受益;XML 级 prompt 全可控故零成本。落点:Phase B line 101(CC 激进段保留 + caching 段重写)。
- 2026-06-18 **简化:L1 工具索引 name 全注入,撑爆靠 disable+skill enable、删折叠机制(用户)**。原 Phase B「单元非 discovery 边界」② 的「按 unit 大小自动展开/折叠 + `default_enabled`/visibility 延伸」搞复杂了 → 删。改:**enabled 工具 name 全注入**(tool-set = set desc + 成员光名字),怕大 unit(50 工具 MCP)撑爆就 **agent MD 配 `disabled`、skill 按需 enable**(复用决策 11 轴 B,激活进 reminder 历史层)—— L1 注入量交 operator 显式 enable/disable 控制,系统不做智能折叠、无新机制。落点:Phase B 该 bullet ②。
- 2026-06-18 **defer = 显式声明、不按 token 自动(用户拍)**。Phase B「到时再敲定」的 deferred 阈值敲定:**config 配 `defer: true` 才 defer(只出 name),没配则完整描述照注入**;不学 CC `tst-auto` 按 token 预算自动 defer —— 私有化无 tokenizer 算不了预算 + 合原则 7(operator 显式配)。落点:Phase B line 101 + 删「到时再敲定」该条。
- 2026-06-18 **`EffectiveToolset` resolver 骨架从 C 提前到 B + 「单元展开(tool-set)」从 G 泛化清单移回 B(用户提:resolver 泛化是基础设施、要不要提前)**。拆「resolver 泛化」发现它非一块:**resolver 抽象本身(唯一解析点、4 读点[`ctx:89`/`ctx:204/215`/`engine:759`/`engine:844`]读同一输出)= 纯基础设施,该在 B 立** —— B 是第一个改工具集形状的阶段(tool-set 单元 + deferred),收口拖到 C/G 则同 4 个读点被 refactor 三轮(B 改渲染 / C 加 active_skills / G 加 dept)= 「同形改动反复 → 退回架构、第 2 轮就退」信号。改后**每阶段只加一个输入层、不重碰读点**:B 立骨架(静态输入 = agent MD 成员 + tool-set 单元展开)→ C 加 `active_skills` → F 加 MCP 运行时单元 → G 加 dept 减法(唯一减法输入、按 `<unit>__*` 前缀整组扣)。**附带纠错配**:「单元展开(tool-set)」本是静态、tool-set 工具要可调本就必须展开,却被列进「C/G 归 G 泛化」清单 → 归 B(MCP 动态单元展开仍在 F)。真正留 G 的只剩 dept 减法语义 + 四类 polymorphic grant 消费 + admin UI(依赖 `department_resource_rule` 消费者 + UI,非 B/C 跑起来需要的基础设施)。落点:Phase B 新增 resolver 骨架 bullet + Phase C line 119 改述「加输入层」+ Phase G line 167 改述「只加 dept 减法输入」。
- 2026-06-18 **部门授权方向归单元 `visibility`、表中性化、tool-set/mcp 补 `visibility` 字段(用户拍,收敛过 deny↔grant↔visibility 三轮)**。起因:reviewer OQ2 指"同表 skill=grant、tool=deny 反语义会坑 UI/API"。讨论先后试"整表 deny"(砍掉"限定给某部门"用例)、"整表 grant"(砍掉"大家用、收回某部门"用例)——两个单方向都逼出枚举全树。**关键洞察(用户提)**:工具缺 skill 那样的 `visibility` 字段,resolver 无从知道一行该加还是减。**定稿**:① `visibility` 升为 **skill/tool-set/mcp 三类共享字段**(tool-set/mcp 写各自 config frontmatter);② **一行的方向由目标单元 `visibility` 定**——`public`(默认 allow)→ 行=减、`department`(默认 deny)→ 行=加,一行永远="该部门子树是此单元默认姿态的例外";③ **机制[祖先链树解析]本就统一**(决策 10"指父覆盖子树"),两方向各只需 1 行、枚举地狱消失;④ 表中性命名 `department_resource_rule`(原 `department_grant`,grant 偏向已不准,全局重命名);⑤ 散装 builtin 无单元承载 `visibility`、恒 `public`,做部门专有就编进 tool-set。落点:决策 1(visibility 共享)+ 决策 10(方向归 visibility、表改名)+ 决策 11 ③(减法→grant/deny 例外)+ Phase B line 107 / F line 157(各自加 visibility 字段)/ G line 168(resolver dept 输入双向)。**到时再敲定**:树规则 override 优先级(深层翻案祖先,v0 不做、祖先一刀覆盖子树)。
- 2026-06-18 **reviewer 反馈处理一轮(8 条,用户逐条评估后拍)**。**P0 私有 skill 提权 = 降级为明示边界、不加机制**:私有 skill 只能翻开 agent MD 已声明的 `disabled` 工具(引不进 `absent`、碰不了等级),硬拒=`absent`、敏感靠 `confirm`(不可改、用户在环)→ 无法静默提权,verify 非权限边界;残留(无 per-skill ACL)窄、留将来不造 trust-tier 状态机(决策 11 补边界段)。**P1 命名保留 `mcp__`**(推翻 changelog 196"两段/不做 mcp__"):tool-set 与 MCP server 名皆 operator 任取,两段会撞(`github__*`),`mcp__github__*`+`github__*` 天然不撞 + 生态标准;重述为"tool-set 不加冗余 `set__`、MCP 保留 `mcp__` 命名空间",prefix 单元 = `mcp__<server>` 或 `<setname>`(body 本就用 `mcp__server__tool`、无需改,仅此日志反转 196)。**P1 `search_tools` 过滤可见集**:registry 全注册是存储、工具结果必须套 resolver 可调集(排 disabled/absent/已减/未授,含 enabled-but-deferred)→ 成为 resolver 第 5 读点(Phase B line 105)。**P1 B/G grant 边界**:B 只加 visibility 字段 + 稳定 name、不认部门规则,消费全留 G(line 107,修与 line 101 矛盾)。**P1 web_fetch SSRF**:旁路插在 Jina 前会绕过 `_fetch_single_url:202` 的直连前 `validate_public_url`(`_fetch_pdf:367` 自身不校验)→ 旁路必须自校验 + `allow_redirects=False` + 302→内网/元数据测试(line 91/93)。**P2 grammar 不降权**:`generate_tool_instruction` 拆两段,`<format>` 协议语法(`xml_formatter.py:17-42`)留前缀可缓存、只挪逐工具 catalog(line 103)。**P2 D 依赖 A 矛盾**:改"D 依赖 C+沙盒,仅 backend tool→artifact→skill 场景依赖 A"(line 37)。**P2 旧风险同步**:line 180 删"已发现集追踪须自建"(与决策 2 矛盾)。**OQ1**:`disable`→`disabled`,补成员态 schema(旧 auto/confirm=enabled、`disabled` 唯一禁用值、未知值启动 loud-fail,决策 11)。
- 2026-06-19 **reviewer 二轮(5 条,用户拍)+ MCP 命名二次反转回统一 `<unit>__<tool>`**。**命名(用户指示,推翻上轮 213 的"保留 `mcp__`")**:`mcp__server__tool` 有**两个 `__`** → unit 必须是 `mcp__server`(自带 `__`),`<unit>__*` 通配无法干净匹配/解析。定:**统一 `<unit>__<tool>`,`__` 唯一分隔符、恰好一次,unit/tool 名只用单 `_`、都不得含 `__`**;MCP 工具 = `<server>__<tool>`、**不带 `mcp__` 前缀**;**resolver/config 只按已知 unit 名 prefix 匹配、不 split `__` 反推**(reviewer P2#3);撞名风险(tool-set 名 vs MCP server 名)由 **registry 启动期全局唯一 loud-fail** 消解(可强制、非仅契约 → 答上轮我提的 collision 顾虑)。落点:决策 11 单元段 + Phase B line 100 + F line 155/106。**P1 visibility 翻转坑(reviewer)**:方向由 `visibility` 动态定 → 改 visibility 会把旧规则行语义反转(`public` 的 deny 行切到 `department` 变 grant)。定 **Manager 层不变量**:有规则行时**阻断切 visibility**,要求 admin 先清空/迁移 + 确认(决策 10)。**P2 `private` 规则语义(reviewer)**:`private`=owner-only 无部门语义 → **不进 `department_resource_rule`**,Manager/UI 硬拒(决策 1/10)。**P2 决策 2 stale**:删"system prompt 恒为索引行"(catalog 已挪 reminder、只 grammar 留前缀,line 69→103)。**P2 决策 6 stale**:line 73"激活时 pip install"→"模型沙盒按需装、非激活自动"(对齐 Phase D/changelog 198,守原则 8)。
- 2026-06-19 **规则方向改存显式 `effect` 列(取代 visibility 派生)+ 规则体检 check 端点(reviewer P1 三轮 + 用户提)**。起因:reviewer 指上轮"改 visibility 阻断"只挡 DB 资源,**config 类 tool-set/MCP 的 visibility 在 config 文件、无 Manager hook、无旧值可比 → 仍会静默翻转**(`public` 的 deny 行改成 `department` 即变 grant、反授权本想禁的部门);且后果比 config 删除留孤儿(无害空转)更重(错的人拿到权限=正确性)。**根因 = 方向是 visibility 的派生态**(派生随 visibility 漂移)。**定稿(退回架构、非再加守卫)**:规则行存**显式 `effect ∈ {grant, deny}` 列**,`visibility` 退化成"未列出部门的默认值";改 visibility 只动默认、**永不翻已有行**(`public`→`department` 时旧 deny 行变冗余、谁都拿不到=过度收紧的安全方向),**DB/config 一视同仁、不需 hook/mirror/snapshot**——比 reviewer 提的 visibility_snapshot/DB-mirror 都轻且彻底。UI 按 visibility 预填 effect、但存显式值抗漂移。撤上轮 214 的 Manager 阻断不变量(被 effect 列取代),保留"`private` 不进表"。**check 端点(用户提,effect 列解锁)**:admin 只读审计扫表报 ① 孤儿(资源已删)② 冗余/失效(effect==visibility 默认方向、多半 visibility 漂移后的 dead row)——派生方向时永远"自洽"测不出漂移,显式 effect 才比得出;**只读非门禁**(正确性已由 effect 按构造保住),清理是独立 admin 动作、config 类不自动删;HA:skill 查 DB 权威、config 查各容器 registry best-effort。落点:决策 10(effect 列 + 方向句 + 不变量 + check 端点指向)/ 决策 11 ③ / Phase C line 116(表签名)/ G line 167-170(表签名 + resolver 方向 + 新增 check 端点 bullet)。**附带清 reviewer 二轮 P2 两条**:F 加**外部 MCP tool 名确定性可逆 sanitize+映射**(`tools/list` 返回名含 `__`/非法字符 → 清洗给模型、保留原名供 `tools/call`,不拒 server/不丢 tool);F `list_changed` 待定项 reframe 为"旧 tool 描述如何过期/自纠"(无已发现集状态机,决策 2)。
<!-- 新日志按日期顺序追加到此行上方 -->
